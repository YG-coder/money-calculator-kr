# 머니계산기 부동산 금융 계산기 확장 계획

> 작성일: 2026-08-25 · 대상 저장소: `fincalc-kr` (머니계산기.kr) · 기준 커밋: `58c8f16`
>
> 이 문서는 **계획 문서**입니다. 계산기 코드는 포함하지 않습니다.
> 블로그 정예화 계획(`PLAN.md`)과 버전업 기록(`claude/UPGRADE-2026-08.md`)을 대체하지 않으며,
> 두 문서와 독립적으로 읽고 실행할 수 있습니다.

---

## 1. 확정된 제품 방향

| 항목 | 결정 |
|---|---|
| 성장 방식 | **신규 사이트 생성이 아니라 머니계산기 확장이 1순위** |
| 주제 포함 판정 | **"핵심 입력값이 돈인가, 물건인가"** — 돈이면 머니계산기, 물건·행정정보면 제외 |
| 신규 소비자 계산기 사이트 | 만들지 않음 |
| 확장 축 | 부동산 **금융**(대출 한도·자금 조달·임대 수익)과 환전 |

이 방향의 실무적 의미는 두 가지입니다.

1. 부동산이라는 카테고리를 통째로 들여오는 것이 아니라, **부동산 거래에서 돈이 움직이는 지점만** 가져옵니다.
2. 이미 8개월치 계산기가 쌓인 코드베이스 위에 얹는 작업이므로, **새로 만드는 것보다 기존 엔진·컴포넌트 재사용률이 높은 순서**가 실행 순서를 정하는 주요 기준이 됩니다.

---

## 2. 범위와 제외 범위

### 2.1 포함 (사용자 확정)

- LTV 계산기
- DSR 계산기
- 부동산 실투자금 계산기
- 전세 관련 계산기
- 월세 관련 계산기
- 임대수익률 계산기
- 환전 계산기

### 2.2 제외 (사용자 확정)

청약 정보 · 재개발/재건축 **정보** · 부동산 시세 · 공시가격 · 건축규제

> 참고: 재건축 **분담금 계산기**(`/real-estate/reconstruction-contribution-calculator`)는 이미 구현되어 있고, 입력값이 권리가액·비례율 등 **돈**이므로 위 제외 항목("재개발·재건축 정보")에 해당하지 않습니다. 유지합니다.

### 2.3 이번 작업에서 제외하는 기술 과제

- Tailwind 4 마이그레이션 (현재 3.4.x 유지)
- TypeScript 7 (현재 5.x 유지)
- Prettier 전체 재포맷 (약 70개 파일 — 별도 커밋으로 분리)
- 불필요한 패키지 업그레이드, 전면 리팩터링

### 2.4 문서 취급 규칙

- `PLAN.md` — 블로그 정예화 계획. 훼손·대체 금지.
- `claude/UPGRADE-2026-08.md` — 버전업 기록. 수정 금지.
- `sources/` — 현재 이 저장소에 존재하지 않음. 향후 생성 시 읽기 전용.

---

## 3. 현재 구현 현황 (저장소 실측)

### 3.1 스택과 빌드

| 항목 | 값 |
|---|---|
| 프레임워크 | Next.js 16.3.1 (App Router) / React 19.2.8 |
| 언어·스타일 | TypeScript 5.x / TailwindCSS 3.4.x |
| 스크립트 | `dev` `build` `start` `lint`(=`eslint .`) `type-check`(=`tsc --noEmit`) |
| 테스트 | **없음** — 테스트 러너·테스트 파일 모두 부재 |
| 빌드 결과 | 전 페이지 정적 생성 (직전 버전업 기록 기준 44개) |
| 브랜치 | `main`, 작업 트리 클린 |
| 커밋 규칙 | `타입(스코프): 한글 설명` — 계산기 1개 = 커밋 1개가 관례 |
| 문서 관례 | 루트 `README.md` 단 하나. `docs/` 디렉터리 없음 |

### 3.2 라우팅 구조

허브 3개 + 그 아래 계산기 페이지 구조입니다. 대출 계산기만 예외적으로 **루트 평면 경로**를 씁니다.

```
/                                   홈
/loan                               대출 허브
  /loan-interest-calculator         (루트 평면 경로)
  /amortization-calculator
  /jeonse-loan-calculator
  /prepayment-calculator
  /dsr-calculator
  /minus-account-calculator
  /refinance-calculator
  /rate-change-simulator
  /fixed-vs-variable-calculator
/real-estate                        부동산 허브
  /real-estate/acquisition-tax-calculator
  /real-estate/jeonse-vs-wolse-calculator
  /real-estate/property-yield-calculator
  /real-estate/reconstruction-contribution-calculator
  /real-estate/jeonse-wolse-conversion
  /real-estate/vacancy-impact
/finance                            금융 허브
  /finance/{deposit, installment-savings, compound, goal-savings,
            deposit-vs-savings, cma-vs-deposit, real-interest-rate,
            inflation, simple-vs-compound}
/blog, /blog/[slug]                 가이드
/about /contact /terms /privacy-policy /disclaimer
```

**계산기 총 24개** (대출 9 · 부동산 6 · 금융 9).

### 3.3 계산기 1개를 구성하는 3층 구조

모든 계산기가 예외 없이 같은 형태입니다. 신규 계산기도 이 형태를 따릅니다.

```
src/app/<route>/page.tsx              서버 컴포넌트
  ├ buildMetadata({slug, title, description, keywords})
  ├ crumbs[]  (부동산은 홈 > 부동산 계산기 > X 3단계)
  ├ EXAMPLES: CalcExample[]   계산 예시 2건
  ├ FAQ: {q,a}[]              3건 내외 → FAQPage JSON-LD 자동 생성
  ├ guide: JSX                "이용 안내" 본문 (공식·개념·한계)
  └ <Suspense><CalcShell ... calculator={<XxxCalc/>} relatedCalcs={...} /></Suspense>

src/components/calculator/XxxCalc.tsx  "use client"
  ├ useCalcState(FIELDS)      URL searchParams ↔ 입력 상태 동기화
  ├ InputField / ResultCard   공통 입력·결과 컴포넌트
  ├ useMemo(...)              순수 함수 호출
  └ 하단 고지 블록            기준일·한계·참고용 문구

src/lib/<domain>.ts                    순수 함수 (사이드 이펙트 없음)
```

`CalcShell`이 브레드크럼, 페이지 헤더, 계산 예시 카드, 이용 안내, FAQ 아코디언, 관련 링크,
그리고 `WebApplication` / `BreadcrumbList` / `FAQPage` JSON-LD를 한 번에 처리합니다.
**신규 계산기에서 이 껍데기를 다시 만들 필요는 전혀 없습니다.**

### 3.4 계산 엔진 (`src/lib/`)

| 파일 | 내용 | 확장에서의 역할 |
|---|---|---|
| `loan.ts` | `formatKRW` `formatUnit` `monthlyRate` `equalPaymentMonthly` `principalFromPayment` `calcAmortization` `calcJeonseLoan` `calcPrepayment` `calcRefinance` `calcRateScenarios` `calcFixedVsVariable` | 포맷·월이율·원리금 역산은 신규 계산기 전부가 재사용 |
| `dsr.ts` | 스트레스 DSR 정책 엔진 v1 (주담대 전용). 지역별 스트레스 금리 상수, `calcDsr`, `estimatePrincipalFromDsr` | **정책 엔진 분리의 기존 모범 사례.** LTV가 그대로 따를 형태 |
| `realEstate.ts` | `calcAcquisitionTax` `calcJeonseVsWolse` `calcPropertyYield` `calcJeonseWolseConversion` `calcVacancyImpact` | 실투자금 계산기가 `calcAcquisitionTax`를 그대로 재사용 |
| `finance.ts` | 예·적금, 복리, 목표저축, 실질금리, 인플레이션 (591줄) | 환전 계산기가 배치될 도메인 |
| `metadata.ts` | `SITE_NAME` `BASE_URL` `buildMetadata()` | 모든 페이지 메타데이터 |

### 3.5 이미 존재하는 "정책값 + 기준일" 패턴

확장 계획의 핵심 원칙(정책값과 계산 엔진 분리, 기준일·출처 표시)은 **이미 두 곳에서 구현되어 있습니다.** 다만 형태가 서로 다릅니다.

`src/lib/dsr.ts`
```
export const DSR_VERIFIED_DATE = "2026-08-08";
export const LOCAL_MORTGAGE_DEFERRAL_UNTIL = "2026-12-31";
const MORTGAGE_VARIABLE_STRESS_RATE: Record<Region, number> = { metro: 3.0, local: 0.75 };
// 파일 상단 주석에 검증일·근거(금융위 보도자료, 은행연합회 공시)·v1 범위 한계·v2 스펙까지 명시
```

`src/lib/realEstate.ts`
```
export const CONVERSION_RATE_INFO = {
  fixedCapPct: 10, baseRatePct: 2.75, legalAddPct: 2.0,
  verifiedAt: "2026-08-15",
  source: "한국은행 기준금리 · 주택임대차보호법 제7조의2 · 시행령 제9조",
};
```

→ 확장 시 **이 두 형태를 하나로 통일**하는 것이 8절의 제안입니다.

> ⚠️ **즉시 확인 필요**: `CONVERSION_RATE_INFO`의 주석에 "다음 금통위: 2026-08-27"이 적혀 있습니다.
> 오늘(2026-08-25) 기준 이틀 뒤입니다. 기준금리가 바뀌면 전월세 전환율 계산기의 법정 상한이 함께 바뀝니다.
> 이 확장 작업과 무관하게 8/27 이후 `baseRatePct`와 `verifiedAt` 갱신 여부를 확인해야 합니다.

### 3.6 계산기를 하나 추가할 때 손대야 하는 등록 지점 6곳

기존 커밋(`feat(realestate): 공실률 영향 계산기 추가` 등)에서 반복적으로 나타나는 체크리스트입니다.

1. `src/app/<route>/page.tsx` — 신규
2. `src/components/calculator/XxxCalc.tsx` — 신규
3. `src/components/calculator/index.ts` — export 추가
4. `src/components/layout/Header.tsx` — `NAV_GROUPS`의 해당 그룹에 항목 추가
5. `src/app/<hub>/page.tsx` — 허브의 `CALCS` 배열에 카드 추가
6. `src/app/sitemap.ts` — `staticPages`에 경로 추가 (priority 0.9 / monthly)

여기에 더해 **`src/components/layout/Footer.tsx` 링크**와 **형제 계산기 페이지의 `relatedCalcs`** 갱신이 따라옵니다. 과거에 `fix(site): 푸터 계산기 링크와 대출 breadcrumb 최신 구조로 정합화` 커밋이 별도로 필요했던 이유입니다.

### 3.7 코드에 기록된 알려진 함정 (신규 계산기가 반드시 피해야 함)

`useCalcState`가 반환하는 `getWon()` / `getNum()`은 `useEffect`로 한 박자 뒤에 갱신되는 `latestStateRef`를 읽습니다. 그래서 **마지막 입력 시점에 stale 값**이 잡힙니다.
`fix(dsr): 마지막 입력 즉시 계산되도록 반응성 수정`, `fix(calc): 대출 3개 계산기 반응성 수정`, `fix(calc): 금융 4개 계산기 반응성 수정` — 같은 버그로 세 번 커밋이 나갔습니다.

현재 우회 방식(`DsrCalc.tsx`가 채택):
```
const won = (key: string) => { const n = Number(state[key]?.raw ?? "0"); return isNaN(n) ? 0 : n * 10_000; };
const num = (key: string) => { const n = Number(state[key]?.raw ?? "0"); return isNaN(n) ? 0 : n; };
```
즉 `useMemo` 안에서는 **현재 렌더의 `state[key].raw`를 직접 읽어야** 합니다.
같은 헬퍼가 여러 계산기에 복붙되어 있으므로, 8절에서 공통화를 제안합니다.

> **PR 0 실측 (2026-08-25)**: 이 함정은 과거형이 아니라 **당시 운영 중인 버그**였습니다.
> 24개 계산기 중 19개는 각자 로컬 헬퍼로 우회하고 있었지만, **5개는 그대로 stale 값을 쓰고
> 있었습니다** — 취득세·부동산 수익률·월세 vs 전세·전세대출·재건축 분담금.
> 실브라우저 확인 결과 취득세 계산기에 5억을 입력하면 **550,000원**(정답 5,500,000원의 1/10)이
> 표시되고 시간이 지나도 교정되지 않았으며, `?price=50000` URL로 직접 들어가면 결과가 아예
> 표시되지 않았습니다. ESLint 도 같은 지점을 `react-hooks/exhaustive-deps` 경고 5건으로
> 가리키고 있었습니다(useMemo 가 deps 의 `state` 를 실제로 읽지 않았기 때문).
> PR 0 에서 5개 모두 `readNum`/`readWon` 으로 교체해 해소했습니다.

기타 관례:
- 금액 입력 단위는 **만원**. 내부 계산은 원. (`kind: "money" | "integer" | "decimal"`)
- 금리 `0%`는 유효 입력이므로 truthy 검사 금지 — `(state.rate?.raw ?? "") !== ""`로 판정.
- `formatUnit()`은 억/만 단위로 축약, `formatKRW()`는 원 단위 전체 표기.

---

## 4. 계산기 후보 목록 — 중복 확인 결과

확정된 7개 후보를 저장소 실측과 대조한 결과입니다. **7개 중 3개는 이미 구현되어 있습니다.**

| 후보 | 저장소 상태 | 판정 |
|---|---|---|
| LTV 계산기 | 독립 계산기 없음. `JeonseLoanCalc`에 사용자가 직접 입력하는 "LTV 비율" 필드가 있을 뿐, 정책 기반 한도 계산 없음. DSR 페이지·대출 허브 문구에서 "LTV는 별도로 확인하라"고 세 군데에서 안내 중 | 🟢 **신규** |
| DSR 계산기 | **이미 구현** — `/dsr-calculator`. 스트레스 DSR v1(주담대 전용), 확인/추정 2모드, 지역·금리유형·상환방식 분기, 정책 상수 검증일 표기까지 완비 | 🔵 **보강** (v2) |
| 부동산 실투자금 계산기 | 독립 계산기 없음. `calcPropertyYield`가 `실투자금 = 매입가 − 보증금 − 대출금`을 계산하지만, 해당 페이지가 **"취득세·중개보수를 반영하지 않으므로 실제 초기 투자금은 계산된 실투자금보다 크다"고 스스로 한계를 명시** | 🟢 **신규** |
| 전세 관련 계산기 | **이미 3개** — 전세대출(`/jeonse-loan-calculator`), 월세 vs 전세(`/real-estate/jeonse-vs-wolse-calculator`), 전월세 전환율(`/real-estate/jeonse-wolse-conversion`) | ⚪ **신규 없음** |
| 월세 관련 계산기 | **이미 3개** — 위 두 개 + 공실률 영향(`/real-estate/vacancy-impact`) | ⚪ **신규 없음** |
| 임대수익률 계산기 | **이미 구현** — `/real-estate/property-yield-calculator` (매입가·자기자본 2개 수익률) + 공실률 보완재 | 🔵 **연동 보강** |
| 환전 계산기 | 저장소 전체에 `환전`/`exchange`/`FX` 문자열 **0건** | 🟢 **신규** |

### 4.1 전세·월세를 새로 만들지 않는 이유

원칙("겹치면 억지로 페이지 수를 늘리지 말고 검색 의도와 사용자 흐름 기준으로 판단")을 적용한 결과입니다.

현재 4개 계산기는 검색 의도가 **이미 서로 다르게** 나뉘어 있고, 코드 주석에도 그 구분이 명시되어 있습니다.

| 계산기 | 사용자 질문 | 겹치지 않는 근거 |
|---|---|---|
| 전세대출 | "보증금 3억이면 대출 얼마 나오고 이자 얼마?" | 대출 상품 관점 |
| 월세 vs 전세 | "둘 중 뭐가 싼가?" | 기회비용 비교 |
| 전월세 전환율 | "이 전환이 법정 상한 안인가?" | 코드 주석: *"calcJeonseVsWolse와 역할이 다르다"* |
| 공실률 영향 | "공실 나면 수익 얼마나 깎이나?" | 코드 주석: *"calcPropertyYield의 공백을 채운다"* |

여기에 새 페이지를 얹으면 서로 카니발라이즈됩니다. **전세·월세 영역의 실제 공백은 계산기가 아니라 내부 링크와 허브 동선**이며, 10절에서 다룹니다.

### 4.2 결론 — 이번 확장의 실제 작업 단위

| # | 작업 | 성격 |
|---|---|---|
| 0 | 공통 컴포넌트·정책 데이터 구조 정비 | 선행 리팩터 (신규 페이지 없음) |
| 1 | **LTV 계산기** | 신규 페이지 |
| 2 | **부동산 실투자금 계산기** | 신규 페이지 |
| 3 | **DSR 계산기 v2** (신용대출·기타대출) | 기존 페이지 보강 |
| 4 | **환전 계산기** | 신규 페이지 (데이터 소스 결정 선행) |
| 5 | 부동산 자금 동선 연결 (LTV → 실투자금 → 수익률) | 내부 링크·쿼리 인계 |

---

## 5. 후보별 상세 명세

### 5.1 LTV 계산기 🟢 신규

| 항목 | 내용 |
|---|---|
| **사용자 목적** | 담보(주택) 기준으로 대출이 얼마나 나오는지 확인. DSR이 **소득 기준 한도**라면 LTV는 **담보 기준 한도**이며, 실제 한도는 두 값의 낮은 쪽이다. 현재 사이트는 소득 축만 있고 담보 축이 비어 있다. |
| **URL** | `/ltv-calculator` (대출 그룹은 루트 평면 경로가 관례) |
| **페이지 제목** | `LTV 계산기 — 주택담보대출 담보 기준 한도` |
| **아이콘** | 🏦 또는 📐 |

**입력값과 단위**

| 필드 | kind | 단위 | 비고 |
|---|---|---|---|
| 주택 가격(담보 평가액) | `money` | 만원 | 필수 |
| 지역 구분 | 토글 | — | 규제지역 / 수도권 비규제 / 지방 — **정책 테이블이 요구하는 축에 맞춰 확정** |
| 주택 보유 수 | 토글 | — | 무주택 / 1주택(처분조건 포함 여부) / 다주택 |
| 생애최초 여부 | 토글 | — | 우대 LTV 적용 대상 |
| 방공제 적용 | 토글 | — | 적용 / 미적용(MCI·MCG 가입) |
| 방공제 지역 | 토글 | — | 최우선변제금이 지역별로 다름 |
| 선순위 채권·임차보증금 | `money` | 만원 | 선택. 기존 근저당·전세보증금 |

**출력값**

- 적용 LTV (%) — 어떤 조건 조합으로 이 값이 나왔는지 함께 표기
- LTV 기준 담보 인정액 = 주택가격 × 적용 LTV
- 정책상 절대 한도 적용 여부 및 적용 후 금액
- 방공제 차감액
- 선순위 채권 차감액
- **최종 담보 기준 추정 한도**
- 필요 자기자금 = 주택가격 − 최종 한도
- 참고: 같은 조건에서 DSR 기준 한도도 확인하라는 안내 + `/dsr-calculator` 링크

**계산 공식**

```
담보인정액 = 주택가격 × 적용LTV(지역, 주택수, 생애최초, 가격구간)
절대한도 적용 = min(담보인정액, 정책상 절대 한도액)   ← 해당 정책이 있는 경우만
최종한도 = max(0, 절대한도 적용값 − 방공제액 − 선순위채권)
```

> ⚠️ **단일 고정 공식으로 단순화 금지.** `적용LTV`는 상수가 아니라 **(지역 × 주택수 × 생애최초 × 가격구간) → 비율** 을 반환하는 조회 함수여야 합니다. `dsr.ts`의 `getEffectiveStressRate()`와 동일한 형태입니다.

**필요한 정책·규제 데이터**

| 데이터 | 성격 | 출처(구현 시 원문 확인 필수) |
|---|---|---|
| 지역·주택수·생애최초별 LTV 비율표 | 수시 변동 (가계부채 관리방안마다 개정) | 금융위원회 보도자료, 은행연합회 공시 |
| 규제지역(조정대상지역·투기과열지구) 지정 현황 | 수시 변동 | 국토교통부 고시 |
| 정책상 대출 절대 한도액 | 수시 변동 | 금융위원회 |
| 방공제 = 소액임차보증금 최우선변제금 (지역별) | 개정 빈도 낮음 | 주택임대차보호법 시행령 |

> 이 문서는 **정책 수치를 확정해 적지 않습니다.** 착수 시점에 위 출처 원문으로 확인해 `src/lib/policy/ltv.ts`에 `verifiedAt`과 함께 기록하는 것이 이 계산기의 실제 작업 대부분입니다. 근거 없는 수치를 코드에 넣는 것이 이 계산기에서 가장 큰 위험입니다.

**데이터 기준일 표시 및 갱신**

- 결과 하단에 `기준일: YYYY-MM-DD · 출처: …` 고정 표기 (필수)
- 규제지역 지정은 고시일 기준이므로 **"현재 지정 현황은 국토부 고시로 확인"** 문구 병기
- 갱신 트리거: 가계부채 관리방안 발표, 규제지역 지정·해제 고시
- 갱신 주기: 정기 없음 — **이벤트 기반**. 문서 12절에 점검 항목으로 등록

**예외 상황과 검증 규칙**

| 상황 | 처리 |
|---|---|
| 주택가격 미입력/0 | 결과 미표시 (기존 계산기 관례: 필수값 없으면 `null` 반환) |
| 방공제 + 선순위 > 담보인정액 | 최종 한도 `0`으로 클램프 + "담보 기준 가용 한도 없음" 안내 |
| 선순위 채권 > 주택가격 | 입력 오류로 보고 `InputField` `error` 표시 |
| 다주택 + 규제지역 등 LTV 0% 조합 | 0으로 계산하되 "해당 조건은 담보대출 취급이 제한될 수 있음" 문구 |
| 조건 조합이 정책 테이블에 없음 | 임의 보간 금지 — "이 조합은 지원하지 않음" 명시 (`dsr.ts`가 혼합형·주기형을 명시적으로 미지원 처리한 방식과 동일) |

**기존 계산기와 공유할 수 있는 요소**

- `CalcShell` · `InputField` · `ResultCard` · `useCalcState` — 그대로
- `formatUnit` / `formatKRW` (`lib/loan.ts`) — 그대로
- `ToggleGroup` — 현재 `DsrCalc.tsx` **내부에 정의**되어 있음 → PR 0에서 공통 컴포넌트로 승격 필요
- 정책 엔진 패턴 — `lib/dsr.ts` 구조를 그대로 복제
- 고지 블록 문구 톤 — `DsrCalc`의 하단 3문단 패턴 재사용

**SEO 검색 의도와 내부 링크**

- 의도: "내 조건에서 주담대가 얼마 나오나" — 거래 직전 단계의 높은 구매 의도
- 내부 링크(계산기): DSR 계산기 / 원리금상환 계산기 / 대출이자 계산기 / 부동산 실투자금 계산기
- 내부 링크(가이드): `/blog/loan-interest-calculation` (공개 글)
- 역링크 추가 필요: `/dsr-calculator`의 `relatedCalcs`, `/loan` 허브 `CALCS`, `/real-estate` 하단 "대출 관련 계산" 블록

**구현 난이도 / 정책 변경 위험**

| | 평가 |
|---|---|
| 구현 난이도 | **중~상** — 계산 자체는 단순, 정책 테이블 설계·검증이 작업의 80% |
| 정책 변경 위험 | **최상** — 확장 후보 중 가장 자주 바뀜 |
| 완화책 | 정책값 100% 분리 + `verifiedAt` 노출 + "참고값" 고지 + 조합 미지원 명시

---

### 5.2 부동산 실투자금 계산기 🟢 신규

| 항목 | 내용 |
|---|---|
| **사용자 목적** | "매매가 6억이면 현금 얼마 있어야 하나." 매매가만 보고 자금 계획을 세우면 취득세·중개보수·등기비용이 빠진다. 기존 `property-yield-calculator`가 **자기 페이지에서 명시적으로 비워둔 공백**을 정확히 메운다. |
| **URL** | `/real-estate/initial-cost-calculator` |
| **페이지 제목** | `부동산 실투자금 계산기 — 취득세·중개보수 포함 총 필요자금` |
| **아이콘** | 💰 |

**입력값과 단위**

| 필드 | kind | 단위 | 비고 |
|---|---|---|---|
| 매매가 | `money` | 만원 | 필수 |
| 주택 보유 수 | 토글 | — | `OwnershipType` 재사용 (first/second/third/fourth_plus) |
| 조정대상지역 | 토글 | — | 취득세 중과 판정 |
| 전용 85㎡ 초과 | 토글 | — | 농어촌특별세 과세 기준 |
| 대출금 | `money` | 만원 | 선택. LTV 계산기에서 쿼리로 인계 가능 |
| 임대보증금 | `money` | 만원 | 선택. 갭투자·임대승계 |
| 중개보수 | 자동/직접 토글 + `money` | 만원 | 자동 = 요율표 상한, 직접 = 실제 협의 금액 |
| 등기·법무 비용 | `money` | 만원 | 선택. 기본은 직접 입력 (아래 주석 참조) |
| 기타 비용 | `money` | 만원 | 선택. 이사·수리 등 |

**출력값**

- 취득세 · 농어촌특별세 · 지방교육세 · **취득세 합계** (기존 `calcAcquisitionTax` 결과 그대로)
- 중개보수 (자동 산정 시 적용 요율·한도액 함께 표기)
- 등기·법무 비용, 기타 비용
- **총 부대비용** 및 매매가 대비 비율(%)
- **총 필요자금** = 매매가 + 총 부대비용
- **실투자금(자기자금)** = 총 필요자금 − 대출금 − 임대보증금  ← 하이라이트
- 참고: 기존 수익률 계산기로 이어지는 링크

**계산 공식**

```
취득세합계   = calcAcquisitionTax(매매가, 주택수, 조정지역, 85㎡초과).totalTax   ← 기존 함수 재사용
중개보수     = min(매매가 × 상한요율(거래구간), 구간 한도액)                      ← 자동 모드
총부대비용   = 취득세합계 + 중개보수 + 등기법무비용 + 기타비용
총필요자금   = 매매가 + 총부대비용
실투자금     = 총필요자금 − 대출금 − 임대보증금
부대비용비율 = 총부대비용 ÷ 매매가 × 100
```

**필요한 정책·규제 데이터**

| 데이터 | 성격 | 현재 상태 |
|---|---|---|
| 주택 취득세율 (1주택 구간 누진, 중과, 농특세, 지방교육세) | 세법 개정 시 변동 | ✅ **이미 구현·검증됨** — `realEstate.ts`, 2026-08-02 지방세법 기준 |
| 중개보수 상한요율표 (매매/임대차, 거래금액 구간별, 한도액) | 개정 빈도 낮음 | ❌ 신규 필요. 출처: 공인중개사법 시행규칙 별표 — **지자체 조례로 세부가 달라질 수 있음을 고지** |
| 국민주택채권 매입률·즉시매도 할인율 | **일 단위 변동** | ❌ v1에서 **자동 계산하지 않음** (아래 참조) |

> **등기비용을 자동 계산하지 않는 이유**: 등기 비용의 큰 축인 국민주택채권 할인율은 매일 바뀌고 공신력 있는 정적 소스가 없습니다. 매일 바뀌는 값을 정적 사이트에 상수로 박으면 그날부터 틀린 계산기가 됩니다. v1은 **사용자 직접 입력 + 개략 범위 안내**로 처리하고, 안내 문구에서 "법무사 견적 기준" 임을 명시합니다.

**데이터 기준일 표시 및 갱신**

- 취득세: 기존 검증일(2026-08-02) 승계, 결과 하단 표기
- 중개보수: 요율표 기준일 + "지자체 조례에 따라 다를 수 있음" 병기
- 갱신 트리거: 지방세법 개정(취득세), 공인중개사법 시행규칙 개정(중개보수)

**예외 상황과 검증 규칙**

| 상황 | 처리 |
|---|---|
| 실투자금 ≤ 0 | **정상 표시하되** "대출금과 보증금 합이 총 필요자금을 넘습니다 — 입력을 확인하세요" 안내. `PropertyYieldCalc`의 `isInvestedNegative` 패턴 재사용 |
| 대출금 > 매매가 | `InputField` `error` 표시 |
| 매매가 미입력 | 결과 미표시 |
| 중개보수 자동 모드에서 구간 경계 | 요율표 구간 경계 처리를 테스트 케이스로 명시 |

**기존 계산기와 공유할 수 있는 요소**

**확장 후보 중 재사용률이 가장 높습니다.** `calcAcquisitionTax`를 인자 그대로 호출하므로 취득세 로직을 다시 짤 필요가 없고, 취득세 계산기와 입력 축(주택수·조정지역·85㎡)이 완전히 동일합니다.

**SEO 검색 의도와 내부 링크**

- 의도: "집 살 때 현금 얼마 필요한가" — 매수 검토 초기~중기
- 내부 링크(계산기): 취득세 계산기 / LTV 계산기 / 부동산 수익률 계산기 / 전세대출 계산기
- 내부 링크(가이드): `/blog/acquisition-tax-guide`, `/blog/property-yield-guide` (둘 다 공개 글)
- 역링크 필수: `property-yield-calculator` 페이지의 "취득비용 미반영" 한계 문단에서 이 계산기로 링크 — 기존 문서의 공백을 링크로 메우는 형태

**구현 난이도 / 정책 변경 위험**

| | 평가 |
|---|---|
| 구현 난이도 | **중하** — 신규 로직은 중개보수 요율표뿐 |
| 정책 변경 위험 | **중** — 취득세는 기존 관리 체계 안, 중개보수는 개정이 드묾 |

---

### 5.3 DSR 계산기 v2 (신용대출·기타대출) 🔵 보강

| 항목 | 내용 |
|---|---|
| **사용자 목적** | 현재 v1은 주담대만 계산한다. 실제 DSR은 신용대출·전세자금대출을 합산해야 나오므로, 신용대출이 있는 사용자에게 현재 결과는 과소 추정이다. |
| **URL** | 기존 `/dsr-calculator` — **신규 페이지 없음** |
| **작업 성격** | 엔진 확장 + 입력 UI 추가 |

**이미 저장소에 스펙이 작성되어 있습니다.** `src/lib/dsr.ts` 상단 주석의 "v2 신용대출 지원 시 필요" 항목(검증일 2026-08-08, 출처 금융위 3단계 시행방안)을 그대로 구현 사양으로 사용합니다.

1. 게이팅 — 신용대출 총잔액(기존+신규)이 1억원 초과일 때만 스트레스 적용
2. 스트레스 배율 — 변동 100%, 3~5년 미만 고정 60%, 5년 이상 고정 0%
3. 원리금 산정 — 실제 약정만기·상환방식 무시, **일괄 5년 만기 원금균등**
4. 전세자금대출 — 원금 제외, **이자만** DSR 반영
5. 추정 가능액 모드 — 1억원 경계 전후로 스트레스 적용이 갈리므로 **두 구간 분기 계산**

**입력 추가**: 신용대출 기존 잔액(만원) / 신규 신용대출(만원) / 신용대출 금리(%) / 신용대출 금리유형(변동·3~5년 고정·5년 이상 고정) / 전세자금대출 잔액·금리

**출력 변경**: 대출 종류별 연간 원리금 기여도를 분해해 표시 (주담대 / 신용 / 전세자금)

**예외**: 1억원 경계에서 결과가 계단식으로 점프하므로 "1억원 초과 시 스트레스가 적용되어 한도가 급감한다"는 설명을 UI에 명시 — 사용자가 버그로 오인하기 가장 쉬운 지점.

**재사용**: `calcNewLoanAnnualDebtService`, `principalFromPayment`, 기존 `DsrCalc` UI 전체.

**SEO**: 신규 URL이 없으므로 **검색 유입 증가 기대 없음.** 기존 페이지의 정확도·신뢰도 개선이 목적. 이 점 때문에 우선순위가 신규 페이지들보다 뒤로 갑니다.

**난이도 중 / 정책 변경 위험 최상** (스트레스 DSR 단계는 계속 조정 중, 지방 유예 종료 2026-12-31 예정)

---

### 5.4 환전 계산기 🟢 신규

| 항목 | 내용 |
|---|---|
| **사용자 목적** | "100만원 환전하면 달러 얼마 받고 수수료 얼마 떼나", "환율우대 90%면 얼마 아끼나". 매매기준율이 아니라 **현찰 살 때 환율 + 우대율**이 실제 지불액을 정한다. |
| **URL** | 8절/11절의 배치 결정에 따름 (`/finance/exchange` 또는 `/exchange-calculator`) |
| **페이지 제목** | `환전 계산기 — 환율우대 적용 실제 환전 금액` |

**입력값과 단위**

| 필드 | kind | 단위 |
|---|---|---|
| 환전 방향 | 토글 | 원화 → 외화 / 외화 → 원화 |
| 통화 | 토글/셀렉트 | USD, JPY, EUR, CNY … |
| 금액 | `money` | 원 또는 해당 통화 |
| 매매기준율 | `decimal` | 원 |
| 현찰 스프레드 | `decimal` | % (통화별 기본값 제시, 수정 가능) |
| 환율우대율 | `decimal` | % (0~100) |

**출력값**: 적용 환율 / 받는 금액 / 지불 금액 / 환전수수료 총액 / 우대로 절약한 금액 / 우대 0% 대비 차이

**계산 공식**

```
현찰살때  = 매매기준율 × (1 + 스프레드/100)
적용환율  = 매매기준율 + (현찰살때 − 매매기준율) × (1 − 우대율/100)
지불원화  = 외화금액 × 적용환율
수수료    = (적용환율 − 매매기준율) × 외화금액
절약액    = (현찰살때 − 적용환율) × 외화금액
```
(외화 → 원화는 `현찰팔때 = 기준율 × (1 − 스프레드/100)`로 부호 반전)

**필요한 데이터 — 이 계산기의 유일한 난점**

환율은 실시간 데이터이고, **현재 사이트는 전 페이지 정적 생성**입니다. 세 가지 선택지가 있고 각각 비용이 다릅니다.

| 안 | 방식 | 장점 | 비용·위험 |
|---|---|---|---|
| **A. 사용자 직접 입력** | 매매기준율을 사용자가 은행 앱에서 보고 입력 | 인프라 변경 0, 정적 유지, 값이 틀릴 위험 없음 | 입력 마찰 1단계 추가 |
| **B. 빌드타임 fetch + ISR** | 한국수출입은행 등 공개 API를 `revalidate`로 주기 갱신 | 사용자 마찰 없음 | 정적 전제 이탈, API 키 관리, 배포 인프라 확인 필요, 장애 시 오답 노출 |
| **C. 클라이언트 fetch** | 브라우저에서 직접 호출 | 항상 최신 | CORS·키 노출·rate limit, 정적 사이트에 부적합 |

**권장: v1은 A안.** 이유 — (1) 인프라를 건드리지 않아 이번 확장 범위 안에 들어옴, (2) 이 계산기의 사용자 질문은 "지금 환율이 얼마냐"가 아니라 "**우대율을 적용하면 얼마 아끼냐**"이므로 기준율은 입력값이어도 효용이 유지됨, (3) 틀린 환율을 자동으로 보여주는 것보다 사용자가 넣은 값으로 계산하는 쪽이 신뢰 위험이 낮음.
→ **최종 결정은 11절 질문 Q3.**

**기준일 표시**: A안에서는 "입력하신 기준율 기준 계산" 문구 + 통화별 스프레드 기본값의 기준일·출처 표기.

**예외**: 우대율 > 100% 또는 < 0 → `error`. 기준율 0 이하 → 결과 미표시. 금액 0 → 미표시. JPY는 100엔 단위 고시라는 점을 **반드시** 반영(가장 흔한 계산 오류).

**재사용**: `CalcShell` `InputField` `ResultCard` `useCalcState` — UI는 전부 재사용. 계산 엔진은 신규(`lib/exchange.ts`).

**SEO**: 의도는 "환전 수수료 아끼는 법 / 환율우대 계산". 기존 계산기 중 직접적 연결 대상이 적어 **내부 링크 관점에서 고립되기 쉬움** — 실질금리·인플레이션 계산기와 "돈의 실질 가치" 축으로 묶는 것이 현실적.

**난이도 하(A안) / 정책 변경 위험 없음 / 데이터 신선도 위험 최상**

---

### 5.5 부동산 자금 동선 연결 🔵 보강 (신규 페이지 없음)

세 계산기를 **하나의 흐름**으로 잇습니다.

```
LTV 계산기            "얼마 빌릴 수 있나"      → 대출금
   ↓ 쿼리스트링 인계
실투자금 계산기        "현금 얼마 필요한가"     → 실투자금
   ↓ 쿼리스트링 인계
부동산 수익률 계산기    "그래서 수익 나나"
```

`useCalcState`가 이미 URL searchParams와 양방향 동기화하므로, **"이 값으로 다음 계산기 열기" 링크 한 줄**로 구현됩니다. 새 인프라가 필요 없습니다.
동시에 DSR ↔ LTV를 "소득 기준 한도 ↔ 담보 기준 한도"로 상호 링크해 두 축을 완성합니다.

---

## 6. 우선순위 표

평가 기준 6개를 5점 척도로 매겼습니다. **유지보수 부담은 낮을수록 높은 점수**(부담이 적을수록 좋음)입니다.

| 작업 | 주제<br>적합성 | 검색 의도·<br>사용자 효용 | 코드<br>재사용성 | 공식 정확성<br>구현 가능성 | 유지보수<br>(높을수록 부담 적음) | 기반<br>기능성 | 합계 |
|---|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| **0. 공통화 (ToggleGroup·policy·state 유틸)** | — | — | 5 | — | 5 | **5** | **기반** |
| **1. LTV 계산기** | 5 | 5 | 4 | 3 | 2 | **5** | **24** |
| **2. 실투자금 계산기** | 5 | 4 | **5** | 4 | 4 | 3 | **25** |
| **3. DSR v2** | 5 | 3 | 5 | 3 | 2 | 2 | **20** |
| **4. 환전 계산기** | 4 | 3 | 3 | 5 | 3 | 1 | **19** |
| **5. 자금 동선 연결** | 5 | 4 | 5 | 5 | 5 | 3 | **27** |

### 6.1 점수만 따르지 않는 이유 — 권장 실행 순서

합계로는 실투자금(25)이 LTV(24)보다 높지만, **권장 순서는 LTV가 먼저**입니다.

- **입력 축을 LTV가 정의합니다.** LTV와 실투자금은 지역·주택 수·조정대상지역이라는 축을 공유합니다. 실투자금을 먼저 만들면 취득세 기준 축(주택수·조정지역·85㎡)만 반영되고, 나중에 LTV를 붙일 때 지역 구분 체계를 다시 손봐야 합니다.
- **실투자금은 LTV 결과를 인계받을 자리가 있습니다.** 반대 방향 의존은 없습니다.
- **기반 기능성이 명시적 평가 기준**입니다. LTV는 담보 기준 한도라는 축 자체를 사이트에 추가하는 유일한 항목입니다.

단, LTV는 **정책값 확정이 선행 조건**입니다(11절 Q1). 정책 확인에 시간이 걸려 착수가 지연될 경우, **실투자금을 먼저 진행하고 LTV를 뒤로 미루는 것도 유효한 대안**입니다 — 그 경우 실투자금의 지역 입력을 취득세 축(조정대상지역 여부)만으로 좁혀 두고, LTV 도입 시 확장하는 것으로 처리합니다.

`5. 자금 동선 연결`은 점수가 가장 높지만 **1·2가 존재해야 성립**하므로 마지막입니다.

---

## 7. 단계별 구현 로드맵

각 단계 = **PR 1개 = 커밋 1개**. 저장소 관례(`타입(스코프): 한글 설명`, 계산기 1개당 커밋 1개)를 그대로 따릅니다.

### ✅ PR 0 — `refactor(calc): 공통 입력 유틸·정책 구조 정비 및 Vitest 도입` (완료 2026-08-25)

신규 페이지 없음. 뒤따르는 모든 PR의 전제.

- [x] `src/lib/calcInput.ts` 신설 — 입력 파싱(`sanitize`/`toDisplay`)과 현재 렌더 기준 읽기 유틸(`readRaw`/`readNum`/`readWon`/`isFilled`). 순수 함수만, React·Next 의존 없음
- [x] `useCalcState`가 위 모듈을 사용하도록 변경하고 유틸을 re-export. **훅 본문 로직은 무변경**(추가된 것은 JSDoc 2줄뿐), `getWon`/`getNum`은 `@deprecated` 표시 후 유지
- [x] `ToggleGroup`을 `DsrCalc.tsx` 내부 → `src/components/calculator/ToggleGroup.tsx`로 승격, `index.ts` export (클래스 문자열 동일 — 디자인 변화 없음)
- [x] `src/lib/policy/types.ts` 신설 — `PolicyMeta` / `PolicyEntry` / `PolicyTable` / `lookupPolicy` / `validatePolicyMeta` / `formatPolicyStamp`
- [x] `lib/dsr.ts` · `lib/realEstate.ts`의 기존 정책값은 **이전하지 않음** (동작 검증된 계산기를 건드리지 않기 위해)
- [x] Vitest 최소 도입(devDependency 1개) + 34개 단위 테스트
- [x] stale 입력을 실제로 쓰고 있던 계산기 5개에 새 유틸 적용 (아래 3.7절 참조)

검증: `vitest run` 34/34 · `tsc --noEmit` 0 · `eslint .` 에러 0(경고 7→2) · `next build` 44 페이지 · 실브라우저 before/after 스모크

### ✅ PR 1 — `feat(loan): LTV 계산기 추가` (완료 2026-08-25)

- [x] `src/lib/policy/ltv.ts` — LTV 9조합 표 + 절대한도 + 방공제 참고금액, `PolicyMeta` 3종
- [x] `src/lib/ltv.ts` — `calcLtv()` 순수 함수. 산식 B (`max(0, min(LTV한도 − 선순위 − 방공제, 절대한도))`)
- [x] `src/lib/ltv.test.ts` — 18개. A/B 판별 2건 + 동일 2건 + 클램프 + 구간 경계 + 게이팅
- [x] `src/components/calculator/PolicyNote.tsx` — 기준일·출처·제외 범위 고지 (PR 0에서 이월)
- [x] `src/components/calculator/LtvCalc.tsx` — 방공제 3상태 명시 선택
- [x] `src/app/ltv-calculator/page.tsx` — 메타데이터·예시 2건·FAQ 4건·가이드
- [x] 등록 8곳: `index.ts` · Header · `/loan` 허브 · `sitemap.ts` · Footer · `/dsr-calculator` relatedCalcs + 본문 링크
- [x] `relatedGuides` 는 공개 확인된 글만 (`/blog/loan-interest-calculation`)

검증: `vitest run` 52/52 · `tsc --noEmit` 0 · `eslint .` 에러 0(경고 2, 기준선 동일) ·
`next build` **45 페이지**(44 → +1) · 실브라우저 스모크 17항목 통과

**정책 근거**: `docs/policies/LTV-POLICY-2026-08.md` (rev.4). 산식 B는 은행업감독업무시행세칙 [별표 18]과
금융위 대출한도 규정을 연립해 도출한 확정 산식입니다.

**설계 결정 (D1~D4)**
- D1 지역 축 3분법, `LtvRegion` 별도 타입 — `lib/dsr.ts` 의 `Region` 재사용 안 함
- D2 규제지역 여부는 사용자 선택. 목록은 기준일(2026-07-01) 붙인 참고 안내
- D3 방공제는 **금액 직접 입력**. 조용히 0원을 기본값으로 두지 않고, `직접 입력` 또는
  `공제하지 않음` 중 하나를 명시적으로 고르기 전에는 계산하지 않음. 0원은 `공제하지 않음`을
  고른 경우에만 허용. 지역별 법정 금액은 입력 보조 버튼으로만 제공
- D4 DSR 은 링크만. 쿼리 인계는 PR 5

### PR 2a — `fix(realestate): 취득세 중과 배제·생애최초 감면 반영`  ※ PR 2 선행

PR 2 가 `calcAcquisitionTax` 를 재사용하므로 먼저 진행합니다. 2026-08-25 재검증에서
**세율은 전부 일치하나 적용 대상 판정 3건이 누락**된 것이 확인되었습니다.

- [x] A 지방 저가주택 중과 배제 (비수도권 시가표준액 2억 이하, 정비구역 제외)
- [x] B 생애최초 취득세 감면 (200만원/300만원 명시 선택, 12억 이하)
- [x] C 일시적 2주택 특례 (3년 내 법정 처분 요건)
- [x] `lib/policy/acquisitionTax.ts` 신설 + 엔진 객체 인자 전환
- [x] `AcquisitionTaxCalc` 조건부 입력 UI
- [x] **회귀 테스트 — A·B·C 미해당 입력에서 기존 값 불변**

**완료 2026-08-25**: 1차 체크리스트를 현행 원문으로 해소하고 11개 회귀·정책 테스트를
추가했습니다. 생애최초 85㎡ 초과는 감면분 농어촌특별세의 세목 결합을 과대·과소 추정하지
않도록 v1에서 자동 감면하지 않고 안내합니다.

### ✅ PR 2 — `feat(realestate): 부동산 실투자금 계산기 추가` (완료 2026-08-25)

- [x] `src/lib/policy/brokerage.ts` — 중개보수 6구간 상한요율표 + 한도액, `PolicyMeta`
      · `basis: nationalCeiling` — **시·도 폴백 구조 만들지 않음**
      · `VERIFIED_LOCAL_ORDINANCES = ["서울특별시", "경기도"]`
      · `effectiveFrom: 2021-12-30` (요율 적용 시작일, 조례 버전 2022-12-30 과 구분)
- [x] `src/lib/initialCost.ts` — `calcInitialCost()`. PR 2a 취득세 엔진 재사용
- [x] `src/lib/initialCost.test.ts` — 22개 (구간 경계·한도액·VAT·명시 선택·취득세 연동)
- [x] `src/components/calculator/InitialCostCalc.tsx` — 등기·법무 3상태 명시 선택
- [x] `src/app/real-estate/initial-cost-calculator/page.tsx`
- [x] 등록 7곳: `index.ts` · Header · `/real-estate` 허브 · `sitemap.ts` · Footer ·
      취득세 페이지 역링크 · **수익률 페이지의 “취득비용 미반영” 문단에 링크**
- [x] `relatedGuides` 는 공개 확인된 글만 (`acquisition-tax-guide`, `property-yield-guide`)

검증: `vitest run` 85/85 · `tsc --noEmit` 0 · `eslint .` 에러 0(경고 2, 기준선 동일) ·
`next build` **46 페이지**(45 → +1) · 실브라우저 스모크 19항목 통과

**정책 근거**: `docs/policies/BROKERAGE-POLICY-2026-08.md` (rev.2)

**설계 결정 (E1~E4)**
- E1 취득세 누락 3건은 **PR 2a 에서 선행 처리**. 이 계산기는 보완된 엔진을 그대로 재사용
- E2 부가세 일반과세 10% 기본 포함 + 제외 토글. 라벨 "예상 최대 중개보수"
- E3 시·도 폴백 없음. 국토부 상한 단일 정책 + 서울·경기 조례 일치 확인 고지
- E4 기타 비용 선택 입력(기본 0). 이사·수리·가전 예시 제공
- 등기·법무 비용은 **명시 선택**(직접 입력 / 포함하지 않음). 항상 발생하는 비용이라
  조용한 0원이 총 필요자금을 과소 표시하기 때문. 자동 계산은 하지 않음(국민주택채권
  할인율 일 단위 변동)

### ✅ PR 3 — `feat(dsr): 신용대출·전세자금대출 DSR 반영 (v2)` (완료 2026-08-25)

착수 전 재검증에서 **v2 스펙 주석의 3항목이 부정확**한 것으로 확인되어,
주석이 아니라 재조사 결과(`docs/policies/DSR-POLICY-2026-08.md` rev.3)를 기준으로 구현했습니다.

- [x] `src/lib/policy/dsr.ts` — 정책 레이어 신설 (커밋 `54c0c49`)
- [x] `src/lib/dsr.ts` — 정책 레이어 연결 + 유예 만료 안전장치 (커밋 `c2cda5b`)
- [x] `DsrCalc.tsx` 입력 확장 + `/dsr-calculator` FAQ·예시 갱신 (커밋 `389eb06`)
- [x] 적격 분할상환 잔액 게이팅 보강

**엔진에 반영된 것**

- 신용대출 1억원 게이팅 — 기존(일시상환·마이너스통장·적격 분할상환) + 신규 **합산** 기준
- 신용대출 금리유형별 적용비율 — 5년↑ 고정 0% / 3~5년 고정 60% / 그 밖 100%
- 신용대출은 **지역 구분 없음** — `getCreditStressRatePct` 가 지역 인자를 받지 않아 타입 수준에서 차단
- 마이너스통장은 **약정한도 전액** 기준, 산정만기 5년
- 전세자금대출 5상태 (미선택 / 없음 / 무주택 / 1주택·수도권 규제 / 그 밖)
- 추정 가능액 모드의 1억원 경계 분기 (경계에서 잘릴 때 그 사실을 표시)

**계산을 차단하는 조건** — 값을 추정하지 않고 사유를 노출

| 조건 | 이유 |
|---|---|
| 전세대출 미선택 | 조용한 0원 금지 |
| 전세대출 '그 밖' | 자동 판정 불가 |
| 분할상환 신용대출 (직접 입력 전) | 실제만기 인정 요건·산식 1차 원문 미확인 |
| 지방 주담대 유예 만료 후 | 이후 기준 미확인 |

**설계상 놓치기 쉬운 지점 3가지 (재작업 시 확인)**

1. **1억원이 두 개다.** 차주단위 DSR 적용 기준(총 대출)과 신용대출 스트레스 게이팅
   (신용대출 잔액)은 값이 같아도 의미가 다르다. 상수를 합치지 말 것.
2. **1.5% 는 상수가 아니라 반기 산정 결과다.** 신용대출·지방 주담대는 모두
   `STRESS_RATE.currentPct` 에서 파생된다. 개별 상수(0.75 등)를 따로 박으면
   다음 반기 발표 때 조용히 틀려진다.
3. **적격 분할상환 잔액은 게이팅에만 합산한다.** 연간 원리금은 사용자가 '기타 부채'에
   직접 입력한다. 이 필드를 비우면 1억원 판정이 **과소** 산정되어 DSR 이 낮게(=한도가
   높게) 나오므로, 신규 대출이 신용대출일 때 조건부 안내를 띄운다.

**검증** — 테스트 158/158, tsc 0, eslint 0, prettier 통과, 빌드 46페이지.
브라우저에서 전세 4경로·신용대출 금리유형·마이너스통장 라벨 전환·분할상환 차단·
URL 상태 복원·2027년 시계 고정 시 지방 차단을 확인.

### ✅ PR 4 — `feat(finance): 환전 계산기 추가` (완료 2026-08-28)

Q3·Q4 확정: 실시간 환율 연동 없음 / URL `/finance/exchange` / 기존 `/finance` 허브에 배치.

- [x] `src/lib/policy/currency.ts` — **통화 고시 단위** 메타데이터
- [x] `src/lib/exchange.ts` — `calcExchange()`
- [x] `src/lib/exchange.test.ts` — 25건
- [x] `src/components/calculator/ExchangeCalc.tsx`
- [x] `src/app/finance/exchange/page.tsx` + 등록 6곳

#### ⚠️ 계획 변경 — `policy/currency.ts` 에서 스프레드 기본값을 뺐다

원래 계획은 "통화별 현찰 스프레드 기본값 + verifiedAt" 이었으나 **폐기**했습니다.
스프레드는 은행·통화·상품·채널마다 다르고 검증된 공통값이 없습니다.
평균값을 심으면 사용자가 손대지 않았을 때 근거 없는 수수료가 조용히 계산에 들어갑니다.

그 파일에 남은 것은 **고시 단위 하나**입니다. 이건 추정값이 아니라 은행 고시 관행이고,
틀리면 결과가 100배 어긋나므로 코드로 고정해야 합니다.

```
100단위 고시: JPY · IDR · VND · KHR   (근거 1차 — 은행 고시환율표)
1단위 고시  : 그 외
```

#### 계산식 (1차 확인)

```
현찰 살 때 = 매매기준율 × (1 + 스프레드율)
현찰 팔 때 = 매매기준율 × (1 − 스프레드율)
우대율 p   → 스프레드율 × (1 − p)
우대 100%  → 매매기준율 그대로
```

우대율은 **환율이 아니라 수수료를 깎는 비율**입니다. 이 오해가 흔해서 가이드·FAQ·
예시 note 세 곳에 같은 설명을 넣었습니다.

#### 계산을 차단하는 조건 — LTV 방공제와 같은 규율

| 조건 | 이유 |
|---|---|
| 매매기준율 미입력 | 실시간 연동이 없으므로 기본 환율·빌드 시점 환율을 쓰지 않는다 |
| 환전 수수료율 미입력 | 검증된 공통값이 없다 |
| 현찰 환율 미입력 (현찰 모드) | 위와 동일 |
| 현찰 환율과 매매기준율의 대소가 방향과 반대 | 두 값이 뒤바뀐 입력 |
| 목록에 없는 통화 | 고시 단위를 모른다 |

우대율만 예외로 **기본 0%** 입니다. 0은 '우대 없음'이라는 실제 상태이고,
0일 때 수수료가 최대라 결과가 보수적입니다. 방공제와 달리 과대 표시 방향이 아닙니다.

#### 스프레드 입력 2방식

은행마다 공시 형태가 달라 명시 선택으로 두었습니다.
`수수료율(%)로` / `현찰 환율로`(스프레드 역산). 두 방식의 결과가 일치하는지 테스트로 고정.

#### 검증

테스트 183/183 (환전 25건 신규) · tsc 0 · eslint 0 · 빌드 **46 → 47페이지**.
브라우저: 미입력 3종 차단, USD 예시1(1,382,415원)·JPY 예시2(953,325원) 재현,
100단위 안내, 현찰 역산, 방향 뒤바뀜 감지, URL 복원, 우대 100% 경로.

> **반올림 함정 재발** — `실질 환율` 표기가 기본 4자리 반올림이라 100단위 통화에서
> 9.53325 가 9.5333 으로 나와 예시 표기와 어긋났습니다. LTV·실투자금에 이어 세 번째입니다.
> 1단위 환산 환율만 6자리로 표기하도록 고쳤습니다. **파생 단위가 바뀌는 값은 표기
> 자릿수를 따로 확인할 것.**

### ✅ PR 5 — `feat(site): 부동산 자금 동선 연결` (완료 2026-08-28)

- [x] LTV → 실투자금 → 수익률 쿼리스트링 인계 링크
- [x] DSR ↔ LTV 상호 링크 (값 인계 없음)
- [x] `/real-estate` 허브에 "부동산 자금 흐름" 3단계 섹션
- [x] `/loan` 허브 가이드 본문에서 두 계산기로 링크
- [x] `src/lib/handoff.ts` + 테스트 12건

#### 인계 규칙

```text
LTV      → 실투자금   price(주택가격) · loan(담보 기준 한도)
실투자금 → 수익률     purchasePrice · loanAmount · deposit
DSR     ↔ LTV        링크만. 값은 인계하지 않는다
```

DSR ↔ LTV 를 링크만으로 둔 이유는 두 계산기가 요구하는 입력이 겹치지 않기
때문입니다. DSR 은 소득·기존 부채, LTV 는 담보가치·선순위채권·방공제가 필요합니다.
넘길 값이 없는데 링크만 걸면 오히려 "값이 이어질 것"이라는 기대를 만들므로,
링크 아래에 값이 이어지지 않는다는 문구를 함께 넣었습니다.

#### 인계하지 않는 것

**금액 필드만 넘깁니다.** 방공제(LTV)·등기비용·중개보수 모드(실투자금)처럼
사용자가 명시적으로 골라야 하는 상태는 쿼리에 넣지 않습니다.
조용한 기본값 금지 원칙이 인계 경로로 우회되면 안 되기 때문입니다.

실제로 인계 직후 실투자금 계산기는 결과를 내지 않고 등기비용 선택을 요구합니다.
(브라우저 확인 완료)

#### ⚠️ 두 계산기의 '실투자금'은 정의가 다르다

```text
실투자금 계산기 : 총필요자금(매매가 + 부대비용) − 대출 − 보증금
수익률 계산기   : 매입가 − 보증금 − 대출금          ← 부대비용 제외
```

그래서 **금액을 그대로 옮기지 않고 입력 3개(매입가·대출·보증금)만 넘깁니다.**
equityWon 을 수익률 쪽에 억지로 맞추려면 매입가를 부대비용만큼 부풀려야 하는데,
그러면 매입가 기준 수익률이 망가집니다.

대신 인계 링크 아래에 두 값의 차이와 금액을 표시했습니다. 자기자본 수익률이
부대비용만큼 **높게** 나온다는 사실을 넘어가기 전에 알립니다.

> **PR 6 에서 해소됨.** 수익률 계산기에 '취득 부대비용' 선택 입력을 추가해
> 두 정의를 일치시켰습니다. 아래 PR 6 참고.

#### 잘못된 쿼리값 무시 — `useCalcState` 보강

인계 링크를 손으로 고친 경우를 막기 위해 URL 파라미터 검증을 추가했습니다.

기존 `sanitize()` 는 "입력 도중"을 다루는 함수라 허용 문자만 남깁니다.
그래서 URL 로 `-5000` 이 들어오면 **부호가 떨어져 5000 으로 계산됐습니다.**
타이핑으로는 만들 수 없지만 URL 로는 만들 수 있는 값이고, 음수를 양수로 바꿔
계산하는 것은 조용한 오답입니다.

`isValidUrlValue()` / `readUrlValue()` 를 추가해 URL 에서 읽을 때만 먼저
검사하고, 통과하지 못하면 그 파라미터를 무시하고 필드 기본값을 씁니다.

```text
무시: -5000 · abc · 1e5 · NaN · Infinity · 정수 필드의 4.5
통과: 5000 · 5,000 · " 5000 " · 소수 필드의 4.5
```

타이핑 경로(`sanitize`)는 건드리지 않았으므로 기존 입력 동작은 그대로입니다.

#### 검증

테스트 201/201 (handoff 12건 · calcInput URL 검증 6건 신규) · tsc 0 · eslint 0 ·
빌드 47페이지.

브라우저로 확인한 것:

| 확인 항목 | 결과 |
|---|---|
| LTV(5억·규제·무주택) → 링크 | `?price=50000&loan=20000` |
| 수신 값 표시·수정 가능 | 50,000 / 20,000 |
| 명시 선택 자동 선택 안 됨 | 결과 미표시, 등기비용 선택 요구 |
| 실투자금 → 수익률 | `?purchasePrice=50000&loanAmount=30000&deposit=5000` |
| 부대비용 차이 안내 | 표시됨 |
| 음수·문자·지수 쿼리 | 기본값으로 무시 |
| 쿼리 없는 진입 | 인계 이전과 동일 |
| DSR ↔ LTV | 양방향 링크, 값 인계 없음 |


### ✅ PR 6 — `feat(realestate): 임대수익률에 취득 부대비용 반영` (완료 2026-08-28)

PR 5 에서 남겨 둔 두 계산기의 '실투자금' 정의 차이를 해소합니다.

- [x] `calcPropertyYield` 에 `extraCostMan` 추가 (선택, 기본 0)
- [x] 자기자본 수익률 분모에 부대비용 포함
- [x] 실투자금 계산기에서 실제 부대비용 인계 (`extraCost`)
- [x] 기존 방식과의 차이를 결과 화면에 표시
- [x] 회귀 테스트 9건

#### 정의 일치

```text
실투자금 계산기 : 총필요자금(매매가 + 부대비용) − 대출 − 보증금
수익률 계산기   : 매입가 + 부대비용 − 보증금 − 대출금   ← 이제 같다
```

#### 기존 결과 보존

`extraCostMan` 은 선택 입력이며 기본 0 입니다. **0 일 때 결과는 이 필드를
추가하기 이전과 완전히 같습니다.** 생략과 `0` 명시가 같은 결과를 내는지도
테스트로 고정했습니다.

#### 부대비용을 넣지 않은 곳

| 지표 | 부대비용 반영 | 이유 |
|---|---|---|
| 자기자본 수익률 | ✅ 분모에 포함 | 실제로 나간 돈이 분모여야 한다 |
| 실투자금 | ✅ 포함 | 위와 같은 정의 |
| **매입가 기준 수익률** | ❌ 미포함 | 이름 그대로 매입가 대비 임대료. 매물 간 비교용 지표 |
| **월·연 순수익** | ❌ 미포함 | 부대비용은 매입 시점의 **일회성** 비용이지 월 운영비가 아니다 |

#### 차이 안내

부대비용을 넣으면 분모가 커져 자기자본 수익률이 **낮아집니다**(= 더 보수적).
사용자가 이전 숫자와 다르다고 오해하지 않도록, 부대비용을 뺀 실투자금과
수익률을 함께 표시합니다.

실측 (3억 · 보증금 5천 · 대출 1.5억 4% · 월세 100 · 관리비 10):

| | 실투자금 | 자기자본 수익률 |
|---|---|---|
| 부대비용 0 | 1억원 | 4.80% |
| 부대비용 2,000만원 | 1억 2,000만원 | **4.00%** |

#### `isInvestedNegative` 판정도 부대비용 기준

보증금 + 대출이 매입가와 같아 기존 정의로는 실투자금 0 이던 경우도,
부대비용을 넣으면 실제로 나간 돈이 있으므로 0 초과가 되어 수익률이 계산됩니다.

#### 검증

테스트 210/210 (수익률 9건 신규) · tsc 0 · eslint 0 · 빌드 47페이지.
브라우저: 부대비용 0 일 때 기존 표기 유지, 2,000만원 입력 시 4.80% → 4.00% 로
낮아지고 비교 수치 노출, 실투자금 → 수익률 인계에 `extraCost` 포함 확인.

### ✅ PR 7 — `feat(policy): 정책 검증일 노후화 개발 경고` (완료 2026-08-28)

- [x] `PolicyMeta.reviewBy` (기계 판독용) 추가. `nextReviewHint` 는 사람용으로 유지
- [x] `getPolicyFreshness(meta, asOf)` — 기준일을 **인자로** 받는다
- [x] `POLICY_REVIEW_ITEMS` — 정책 테이블로 표현되지 않는 개별 점검 항목
- [x] `npm run policy:check` + 테스트 단계 경고
- [x] **사용자 화면 변경 없음**

#### 상태 3단계

| 상태 | 뜻 | 처리 |
|---|---|---|
| `fresh` | 검토 기한 전 | 없음 |
| `reviewDue` | 검토 기한 경과 | **개발 단계 경고만** |
| `expired` | 효력 종료 확인 | 계산 차단 (런타임 `unsupported`) |

#### 사용자 화면에 띄우지 않는 이유

검증일이 오래됐다는 사실만으로 정책값이 틀렸다고 볼 수 없습니다.
"이 값은 오래됐을 수 있습니다" 같은 문구는 근거 없는 불안만 줍니다.
화면 차단은 **효력이 실제로 만료됐거나 지원 불가가 확정된 경우에만** 합니다.

#### `reviewBy` 와 `effectiveUntil` 은 다르다

```text
effectiveUntil  정책의 효력이 끝나는 날. 지나면 계산이 위험하다.
reviewBy        사람이 원문을 다시 확인하기로 한 날. 지나도 값은 대개 유효하다.
```

`expired` 는 **정의상 계산이 위험한 상태**이므로, 그 정책값을 쓰는 엔진은 반드시
런타임에서 차단해야 합니다. `freshness` 모듈은 그 항목을 개발 단계에서 함께
훑는 역할이고 실제 차단은 각 엔진이 담당합니다. **둘이 어긋나면 안 됩니다.**

> ⚠️ 최초 구현에서 이 원칙이 깨져 있었습니다. `dsr-stress` 가 2027-01-01 에
> `expired` 로 잡히는데 런타임 차단이 없어, **만료된 스트레스 금리로 계속
> 계산**할 수 있었습니다. 아래처럼 바로잡았습니다.

##### 반기 스트레스 금리 만료 → DSR 계산 전체 차단

`STRESS_RATE.currentPct` 는 **이번 반기의 산정 결과**입니다. 적용 기간이 끝나면
현행 정책값이 아니므로, 다음 발표값을 확인하기 전에는 계산하지 않습니다.

```text
getMortgageStressRatePct  적용 기간 종료 후 → null (지역 무관, 수도권 포함)
getCreditStressRatePct    적용 기간 종료 후 → null
getEffectiveStressRate    금리유형 무관 차단 (순수고정형 포함)
```

수도권까지 막는 이유는 하한 3.0% 도 강화 대책의 산물이라 새 정책 확인 전에는
유지된다고 단정할 수 없기 때문입니다. 순수고정형의 "스트레스 0" 역시 현행
행정지도의 규율이므로 같이 막습니다.

##### 두 만료가 겹칠 때 사유를 구분한다

2026-12-31 은 반기 종료일이자 지방 유예 만료일입니다. 화면에는 해당하는 사유를
보여줍니다.

| 조건 | 사유 |
|---|---|
| 반기 적용 기간 종료 | 스트레스 금리는 6월·12월 발표. 다음 발표값 확인 전 계산 안 함 |
| 반기는 유효 + 지방 유예만 종료 | 지방 유예 종료. 현재 적용 기준 확인 필요 |

##### 검토 기한 값

| 정책 | reviewBy | 근거 |
|---|---|---|
| DSR | 2026-12-01 | 스트레스 금리 6월·12월 발표. 12월 발표 전 |
| 취득세 | **2026-12-31** | 1월 1일 시행 개정이 많아 **시행 전**에 확인 |
| LTV·절대한도 | 2027-02-28 | 가계부채 관리방안 통상 연 1회 |
| 중개보수·방공제·통화 단위 | 2027-08-28 | 변동이 드물어 연 1회 |

#### 날짜 판정은 기준일을 인자로 받는다

내부에서 `new Date()` 를 쓰면 테스트가 **실행 날짜에 따라 깨집니다.**
`getPolicyFreshness(meta, asOf)` 로 두고, 고정 기준일 회귀를 걸었습니다.

```text
2026-08-28  전부 fresh
2026-12-02  dsr-stress reviewDue
2027-01-01  dsr-stress expired
```

`todayKst()` 는 경고 출력에만 쓰고 단정에는 쓰지 않습니다.

#### 단순 노후화는 빌드를 깨지 않는다

`registry.test.ts` 의 노후화 점검은 `console.warn` 만 하고 단정하지 않습니다.
여기에 `expect` 를 걸면 어느 날 갑자기 빌드가 깨지는데, 검증일이 지났다는 것은
"확인하라"는 신호이지 오류가 아닙니다.

**실패하는 것은 구조 검사뿐입니다** — 메타 유효성, id 중복, `reviewBy` 누락,
`reviewBy > verifiedAt`. 정책을 새로 만들고 `registry.ts` 에 등록하지 않으면
여기서 걸립니다.

#### 개별 점검 항목

DSR 은 한 테이블 안에 만료 시점이 다른 항목이 섞여 있어 `reviewBy` 하나로
표현되지 않습니다. 그래서 별도 목록으로 관리합니다.

| id | 기한 | 런타임 차단 |
|---|---|---|
| `dsr-stress-rate-half` | 2026-12-01 | 없음 |
| `dsr-local-deferral` | 2026-12-31 | **있음** |
| `base-rate-mpc` | 2026-10-22 | 없음 (2차 출처 기반 날짜 — 주석에 명시) |

#### 검증

테스트 242/242 (freshness 15건 · registry 12건 · DSR 만료 6건 신규) · tsc 0 ·
eslint 0 · 빌드 47페이지 · UI 파일 변경 0건.

브라우저로 만료 차단을 확인했습니다.

```text
[2026-08-28] 수도권 정상 계산 (스트레스 DSR 50.3%)
[2027-01-15] 수도권·신용대출 모두 차단, 결과 미표시
             사유: "스트레스 금리 적용 기간(2026-H2, ~2026-12-31)이 끝났습니다…"
```

미래 기준일 시뮬레이션으로 경고 출력을 확인했습니다.

```text
[2026-08-28] 정책 0건 / 개별 0건
[2026-12-02] 정책 1건(dsr-stress reviewDue) / 개별 2건
[2027-01-01] 정책 1건(dsr-stress expired)   / 개별 3건
[2027-09-01] 정책 7건 / 개별 3건
```



---

## 8. 공통 컴포넌트 및 데이터 구조 제안

### 8.1 `ToggleGroup` 승격

현재 `DsrCalc.tsx` 안에만 있습니다. LTV(지역·주택수·생애최초·방공제), 실투자금(주택수·조정지역·85㎡·중개보수 모드), 환전(방향·통화) — **신규 3개 전부가 필요**합니다. 그대로 두면 세 번 복붙됩니다.

```ts
// src/components/calculator/ToggleGroup.tsx  (기존 구현을 그대로 이동)
export default function ToggleGroup<T extends string>(props: {
  label: string; hint?: string; value: T;
  options: { value: T; label: string }[];
  onChange: (v: T) => void; gridClass?: string;
}): JSX.Element
```

### 8.2 정책 데이터 계층 `src/lib/policy/`

현재 정책값이 `dsr.ts`(상수 + 주석)와 `realEstate.ts`(`CONVERSION_RATE_INFO` 객체) **두 가지 다른 형태**로 흩어져 있습니다. 확장하면서 세 번째 형태를 만들지 않도록 하나로 정합니다.

```ts
// src/lib/policy/types.ts
export interface PolicyMeta {
  verifiedAt: string;      // "YYYY-MM-DD" — 원문을 직접 확인한 날짜
  source: string;          // 기관명 + 문서명
  sourceUrl?: string;
  note?: string;           // 유예·경과규정 등
  nextReviewHint?: string; // "지방 유예 종료 2026-12-31", "금통위 후 재확인"
}

export interface PolicyTable<TKey extends string, TValue> {
  meta: PolicyMeta;
  values: Record<TKey, TValue>;
  unsupported?: string[];  // 의도적으로 지원하지 않는 조합 (임의 보간 금지 표식)
}
```

배치:

```
src/lib/policy/
  types.ts        PolicyMeta / PolicyTable
  dsr.ts          스트레스 금리 반기값, 신용대출 적용비율, 게이팅   [PR 3]
  ltv.ts          지역·주택수·생애최초별 LTV, 방공제, 절대한도      [PR 1]
  brokerage.ts    중개보수 상한요율표                              [PR 2]
  currency.ts     통화 고시 단위(1/100). 환율·스프레드는 두지 않음   [PR 4]
```

**이전 규칙**: 기존 `dsr.ts` / `realEstate.ts`의 정책 상수는 **이번 확장에서 옮기지 않습니다.** 동작 변경 위험 대비 이득이 없습니다. 신규 정책값만 새 구조를 쓰고, 기존 값은 다음에 해당 파일을 만질 때 자연스럽게 이전합니다.

### 8.3 `PolicyNote` 컴포넌트 — PR 1로 이월

`PolicyMeta`를 받아 결과 하단 고지 블록을 렌더하는 컴포넌트입니다.
**PR 0에서는 만들지 않았습니다** — 렌더할 실제 정책 데이터가 아직 없는 상태에서 표시
컴포넌트만 먼저 만들면 추측으로 모양을 정하게 되기 때문입니다.
대신 문구 조립에 필요한 `formatPolicyStamp(meta)`를 `lib/policy/types.ts`에 두었고,
PR 1에서 LTV 정책 테이블과 함께 컴포넌트를 만듭니다.

```
formatPolicyStamp(meta)
→ "기준일 2026-01-01 · 최종 확인 2026-08-25 · 출처 …"
   (effectiveUntil 이 있으면 "적용 종료 …"가 사이에 들어감)
```

### 8.4 `useCalcState` stale 값 유틸 — PR 0에서 구현됨

훅 내부 로직은 그대로 두고, 파싱·읽기 순수 함수를 `src/lib/calcInput.ts`로 분리했습니다.
훅이 이를 import해 쓰고 동일 이름으로 re-export하므로 기존 import 경로도 그대로 동작합니다.

```ts
// src/lib/calcInput.ts
readRaw(state, key): string    // 현재 렌더의 raw 문자열, 없으면 ""
readNum(state, key): number    // 숫자. 빈 값·미존재 키·파싱 불가는 0
readWon(state, key): number    // readNum × 10,000 (만원 → 원)
isFilled(state, key): boolean  // 0 은 "입력됨"으로 본다 (금리 0%·수수료 0%)
```

- `getWon`/`getNum`은 제거하지 않고 `@deprecated`만 붙였습니다. 디바운스된 URL 쓰기에서는
  ref 방식이 여전히 올바르고, 남은 계산기 19개는 각자 로컬 헬퍼로 이미 우회하고 있어
  강제로 걷어낼 이유가 없기 때문입니다.
- 신규 계산기는 `useMemo` 안에서 **반드시** `readNum`/`readWon`을 씁니다.
- 로컬 헬퍼를 쓰는 19개 계산기의 정리는 PR 0 범위 밖입니다. 각 계산기를 다른 이유로
  손볼 때 함께 교체하는 편이 안전합니다.

### 8.5 신규 계산기 등록 지점 체크리스트

계산기 하나를 추가할 때 빠뜨리기 쉬운 지점을 모은 것입니다. 과거에 푸터 링크와
비공개 글 링크가 각각 별도 fix 커밋으로 나갔던 자리이므로 **PR마다 이 목록을 복사해
체크**하세요.

**코드**

```
□ src/lib/policy/<name>.ts              정책값 (정책 기반 계산기인 경우)
                                        PolicyMeta 필수 8항목 채우기 —
                                        id / version / effectiveFrom / verifiedAt /
                                        sources / supported / unsupported (+ effectiveUntil)
□ src/lib/<name>.ts                     순수 계산 함수. 정책 테이블은 조회만 한다
□ src/lib/<name>.test.ts                단위 테스트 (경계값 포함)
□ src/components/calculator/<Name>Calc.tsx
                                        "use client"
                                        useMemo 안에서는 반드시 readNum/readWon 사용
                                        (getWon/getNum 은 렌더 중 stale — @deprecated)
                                        0 판정은 truthy 대신 isFilled 사용
□ src/components/calculator/index.ts    export 추가
□ src/app/<route>/page.tsx              서버 컴포넌트
```

**노출·탐색**

```
□ src/components/layout/Header.tsx      NAV_GROUPS 의 해당 그룹에 항목 추가
□ src/app/<hub>/page.tsx                허브의 CALCS 배열에 카드 추가
□ src/components/layout/Footer.tsx      푸터 링크 추가
□ 형제 계산기 페이지의 relatedCalcs      역링크 추가 (한 방향만 걸면 반쪽)
□ src/app/sitemap.ts                    staticPages 등록 (계산기 0.9/monthly)
```

**SEO·구조화 데이터**

```
□ buildMetadata({ slug, title, description, keywords })
   · title 은 "%s | 머니계산기" 템플릿에 들어가므로 사이트명 중복 금지
   · title 형태: "<계산기명> — <핵심 차별점>"
   · description 에 무엇을 계산하고 무엇을 반영하지 않는지 포함
□ breadcrumb        부동산 계산기는 3단계 (홈 › 부동산 계산기 › X) 명시
□ examples          계산 예시 2건 — 수치를 손으로 검산하고 PR 설명에 근거 기록
□ faq               3건 내외 → CalcShell 이 FAQPage JSON-LD 자동 생성
□ CalcShell         WebApplication · BreadcrumbList JSON-LD 자동 처리 (별도 작업 없음)
```

**가이드 링크 — 공개 상태 확인 필수**

```
□ relatedGuides 에 넣기 전 src/data/blogPosts.ts 에서 published 확인
   · published: false 인 글은 사이트·사이트맵에서 제외되므로 링크하면 죽은 링크
   · 대응하는 공개 가이드가 없으면 빈 배열로 둘 것 (relatedGuides={[]})
   · 공개 글 목록은 10.4절 참조
```

**정책 기반 계산기 추가 항목**

```
□ 결과 하단에 기준일·출처 표기 (formatPolicyStamp 활용)
□ 지원하지 않는 조건 조합은 임의 보간 없이 "지원하지 않음" 표시
□ "예상값/참고값/추정치" 표현 — 판정·확정 표현 금지
□ 9.2절 재검증 트리거 표에 항목 추가
```

---

## 9. 정확성·테스트·출처 관리 원칙

### 9.1 표현 원칙 (전 계산기 공통)

- 결과는 **"예상값" / "참고값" / "추정치"** 로만 표기합니다. "확정", "정확한 세액", "대출 가능액"은 쓰지 않습니다.
- **법률·세무·금융 판단을 대신한다고 표현하지 않습니다.** 기존 계산기가 이미 이 톤을 지키고 있습니다 — 전월세 전환율 계산기는 상한 초과를 사실로만 표시하고 위법 여부를 판정하지 않으며, 대환대출 계산기는 유불리를 판정하지 않고 두 관점을 나란히 보여줍니다. **신규 계산기도 판정하지 않습니다.**
- 계산기가 반영하지 **않는** 것을 안내에 명시합니다(예: LTV는 DSR·심사 미반영, 실투자금은 국민주택채권 자동계산 미포함).
- 모든 정책 기반 계산기에 `기준일 · 출처` 고정 표기.

### 9.2 정책값 관리

| 규칙 | 내용 |
|---|---|
| 분리 | 정책값은 계산 함수 안에 상수로 넣지 않고 `policy/` 모듈에서 조회 |
| 검증일 | 값 수정 시 `verifiedAt`을 **같은 커밋에서** 반드시 갱신 |
| 근거 | 파일 상단 주석에 기관·문서명·확인 경로 기재 (`dsr.ts` 형식 준수) |
| 미지원 명시 | 정책 테이블에 없는 조합은 임의 보간 금지 — "지원하지 않음"으로 처리 |
| 재검증 트리거 | 가계부채 관리방안 발표 / 규제지역 고시 / 세법 개정 / 금통위 |

**현재 등록된 재검증 항목**

| 대상 | 파일 | 현재 기준일 | 트리거 |
|---|---|---|---|
| 스트레스 DSR (지방 유예) | `lib/dsr.ts` | 2026-08-08 | 유예 종료 2026-12-31 |
| 전월세 전환율 법정 상한 | `lib/realEstate.ts` | 2026-08-15 | **금통위 2026-08-27 — 임박** |
| 주택 취득세 | `lib/realEstate.ts` | 2026-08-25 | 지방세법·지방세특례제한법 개정 |
| LTV·방공제 | `lib/policy/ltv.ts` (예정) | 미착수 | 가계부채 관리방안, 국토부 고시 |

### 9.2.1 ✅ 재검증 2건 — 처리 완료 (2026-08-28)

2026-08-25 에 절차만 준비해 두었던 2건을 결과 확인 후 처리했습니다.

#### (1) 2026-08-27 금통위 → 전월세 전환율 법정 상한 **(값 변경됨)**

**결과: 기준금리 2.75% → 3.00% (0.25%p 인상, 2회 연속).**

법정 상한 = `min(연 10%, 기준금리 + 대통령령 이율 2%)` 이므로 상한이 **4.75% → 5.00%** 로 올랐습니다.

```ts
// src/lib/realEstate.ts
export const CONVERSION_RATE_INFO = {
  baseRatePct: 3.0,           // 2026-08-27 금통위 인상
  verifiedAt: "2026-08-28",
  // 주석의 다음 금통위: 2026-10-22
};
```

**단일 출처 파생이 실제로 동작했습니다.** 상수 한 줄만 바꾸고 재빌드한 결과:

| | 변경 전 (2.75%) | 변경 후 (3.0%) |
|---|---|---|
| 법정 상한 표기 | 4.75% × 4곳 | **5.00% × 4곳** |
| 예시1 상한 적용 월세 | 39.6만원 | **41.7만원** |
| 예시2 상한 적용 월세 | 79.2만원 | **83.3만원** |
| 페이지 내 `4.75` 잔존 | — | **0건** |

수치는 2026-08-25 에 예측해 문서에 적어둔 값과 정확히 일치했습니다.
`getLegalConversionCapPct()` 로 통합해 둔 효과입니다.

> ⚠️ 다음 금통위는 **2026-10-22** 입니다. 이 날짜의 근거 등급은 2차입니다
> (한국은행 공식 발표는 "1·2·4·5·7·8·10·11월 개최"까지만 1차로 확인).

#### (2) 2026-08-28 공인중개사법 시행령·시행규칙 개정 시행 **(값 변경 없음)**

**결과: 주택 중개보수 상한요율 [별표 1] 변경 없음.** 2026-08-11 공포, 2026-08-28 시행.

개정 내용 3가지 모두 이 계산기의 범위 밖이었습니다.

| 개정 항목 | 영향 |
|---|---|
| 한국공인중개사협회 법정화 | 없음 |
| 공동관리비 확인·설명 의무 | 없음 (중개보수 산정과 무관) |
| 주거용 오피스텔 중개보수 결정방식 명확화 | 없음 (오피스텔은 이미 `unsupported`) |

처리한 것:

- `verifiedAt` 2026-08-25 → **2026-08-28**
- `sources` 에 국토교통부 개정 보도자료 1건 추가
- `note` 에 개정 3항목과 "주택 상한요율 불변" 을 기록
- `nextReviewHint` 에서 "2026-08-28 시행 개정 ... 시행 후 확인 필요" 문구 제거

> ⚠️ 근거 등급 **2차** — 국토교통부 보도자료를 인용한 기사 2건으로 교차 확인했으며,
> 국가법령정보센터 원문(robots 차단)은 대조하지 못했습니다.
> "변경 없음"은 부존재 증명이 아니라 **확인한 자료 범위에서의 결론**입니다.

#### 검증

`npm test` 158/158 · `tsc` 0 · `eslint` 0 · `next build` 46페이지 ·
전환율 페이지에서 5.00% 표기와 41.7/83.3만원 반영 확인.

### 9.3 테스트

**현재 저장소에 테스트 인프라가 전혀 없습니다.** 이번 확장에서 테스트 러너를 도입할지는 11절 Q2입니다.

- 도입하지 않는 경우 — 각 PR의 "계산 예시(EXAMPLES)" 2건이 사실상의 회귀 기준이 됩니다. 예시 수치를 손으로 검산한 뒤 페이지에 박고, 이후 엔진을 고칠 때 이 값이 유지되는지 확인합니다. **PR 설명에 검산 과정을 남깁니다.**
- 도입하는 경우 — Vitest 최소 구성 + `src/lib/*.ts` 순수 함수만 대상. UI 테스트는 범위 밖. 신규 엔진(`calcLtv`, `calcInitialCost`, `calcExchange`)부터 시작하고 기존 24개는 소급하지 않습니다.

**어느 쪽이든 필수 검증 게이트** (버전업 기록에서 검증된 절차와 동일):

```
tsc --noEmit          에러 0
eslint .              에러 0 (경고는 기존 exhaustive-deps 7건 수준 유지)
next build            정적 페이지 생성 성공, 페이지 수 증가 확인
브라우저 스모크        ① 초기 렌더 ② 마지막 입력 즉시 반영(3.7절 함정)
                      ③ URL 파라미터 직접 진입 시 값 복원
                      ④ 뒤로/앞으로 상태 복원 ⑤ 모바일 표 가로 스크롤
```

### 9.4 경계값 검산 목록 (구현 시 반드시 확인)

- LTV: 방공제+선순위 > 담보인정액 / LTV 0% 조합 / 정책 절대한도 경계 정확히 걸치는 값
- 실투자금: 중개보수 요율 구간 경계 / 취득세 6억·9억 경계(기존 누진 구간) / 실투자금 0 및 음수
- 환전: JPY 100엔 단위 / 우대율 0%와 100% / 역방향 환전
- ~~DSR v2: 신용대출 잔액 1억원 정확히 / 1억원 ±1만원~~ → PR 3 에서 회귀 테스트로 고정

---

## 10. ✅ SEO 및 내부 링크 전략 (점검 완료 2026-08-28)

> **점검 결과 요약**
>
> | 항목 | 결과 |
> |---|---|
> | 라우트 vs 사이트맵 | 37/37, 누락 0 |
> | 죽은 내부 링크 | **0건** |
> | 비공개 블로그 글 링크 | **0건** (공개 5/16 유지) |
> | DSR ⇄ LTV 상호 링크 | 양방향 완료 |
> | 신규 계산기 유입 링크 | LTV 2 · 실투자금 2 · 환전 2 — 모두 관련성 높은 페이지 |
>
> **처리한 것**
>
> - 10.3 전세·월세 소그룹 → `/real-estate` 허브에 추가 (4개, 상황별 안내 포함)
> - 10.3 수익률 → 실투자금 링크 → 본문 + `relatedCalcs` 양쪽
> - PR 6 이후 사실과 어긋난 "취득세 미반영" 문구 4곳 정정
>
> **하지 않은 것과 이유**
>
> - **유입 링크 추가 없음** — 세 계산기 모두 유입 2건이고 각각 관련성이 실제로
>   높습니다(LTV ← DSR·실투자금 / 실투자금 ← 취득세·수익률 / 환전 ← 인플레이션·실질금리).
>   숫자를 늘리려고 관련 없는 계산기를 엮으면 링크 품질이 떨어집니다.
> - **단방향 `relatedCalcs` 66건 일괄 수정 안 함** — 사이트 전반의 상태이고 확장
>   PR 이 만든 것이 아닙니다. 니치 페이지가 인기 페이지를 가리키는 형태가 많아
>   일괄 양방향화는 오히려 부적절합니다.
> - **Prettier 전면 정리 보류** — `src` 59개 파일이 미준수 상태입니다(4-space 들여쓰기 등).
>   지금 전면 포맷하면 기능 변경 이력과 blame 이 크게 흐려집니다. 별도 유지보수 작업.
>
> ⚠️ **주의**: `src/app/real-estate/property-yield-calculator/page.tsx` 처럼
> prettier 미준수 파일을 손볼 때 `prettier --write` 를 돌리면 파일 전체가
> 다시 쓰입니다(413줄). 내용만 고치고 포맷은 건드리지 마세요.

> 이 문서는 **검색량·시장 규모 수치를 제시하지 않습니다.** 실측 데이터가 없기 때문입니다.
> 아래는 검색 **의도** 분류와 사이트 내부 구조에 근거한 전략이며, 키워드 우선순위가 필요하면 별도 SERP 검증이 선행되어야 합니다.

### 10.1 검색 의도 매핑

| 신규 페이지 | 사용자가 서 있는 지점 | 사이트 내 기존 대응 |
|---|---|---|
| LTV 계산기 | 매수 결정 직전 — "얼마 빌릴 수 있나" | **없음** (담보 축 공백) |
| 실투자금 계산기 | 매수 검토 — "현금 얼마 필요한가" | 부분 (취득세만 단독) |
| 환전 계산기 | 출국·송금 직전 — "수수료 얼마 아끼나" | **없음** |

### 10.2 페이지 메타데이터 규칙

기존 관례를 그대로 따릅니다.

- `buildMetadata({ slug, title, description, keywords })` — `title`은 `%s | 머니계산기` 템플릿에 들어가므로 사이트명 중복 금지
- `title` 형태: `<계산기명> — <핵심 차별점>` (예: `전월세 전환율 계산기 — 법정 상한 확인`)
- `description`: 2~3문장, 계산기가 **무엇을 계산하고 무엇을 반영하지 않는지** 포함
- 부동산 계산기는 `breadcrumb` 3단계 명시 (홈 › 부동산 계산기 › X)
- FAQ 3건 → `CalcShell`이 `FAQPage` JSON-LD 자동 생성
- `sitemap.ts` 등록: 계산기 `priority 0.9 / monthly`, 허브 `0.8 / weekly`

### 10.3 내부 링크 설계

**추가되는 축 — "한도"**

```
DSR 계산기 (소득 기준 한도)  ⇄  LTV 계산기 (담보 기준 한도)
```
두 페이지가 서로를 `relatedCalcs`에 넣고, 본문에서 "실제 한도는 두 값 중 낮은 쪽"이라고 설명합니다. 현재 DSR 페이지가 이미 세 군데에서 "LTV는 별도 확인" 이라고 안내하고 있으므로, **링크 대상이 생기면 그 문장이 곧바로 살아납니다.**

**보강되는 축 — "부동산 자금 흐름"**

```
LTV → 실투자금 → 수익률 (→ 공실률)
        ↑ 취득세
```
`property-yield-calculator`가 자기 페이지에서 밝힌 "취득세·중개보수 미반영" 한계 문단이 실투자금 계산기로 가는 자연스러운 링크 자리입니다.

**전세·월세 (신규 페이지 없이 링크만)**

전세대출 · 월세vs전세 · 전월세 전환율 · 공실률 4개가 서로 부분적으로만 연결되어 있습니다. `/real-estate` 허브에 **"전세·월세 계산기" 소그룹**을 만들고 4개를 한 묶음으로 노출하면, 새 페이지 없이 이 영역의 내부 링크 밀도가 올라갑니다.

> ✅ **구현 완료 (2026-08-28).** 허브의 `JEONSE_WOLSE_CALCS` 배열.
> 전체 목록(`CALCS`)과 항목이 겹치지만 역할이 다릅니다. `CALCS` 는 전체 목록이고,
> 소그룹은 "전세·월세를 고민 중"이라는 **한 가지 상황**으로 묶은 것입니다.
> 각 카드에 임차인/임대인 중 누구를 위한 것인지 한 줄로 덧붙였습니다.
> 전세대출 계산기는 `/loan` 그룹이라 `CALCS` 에는 없지만 이 맥락에는 들어갑니다.

### 10.4 ⚠️ 가이드 링크 시 반드시 지킬 것

`PLAN.md`의 블로그 정예화 결과, **블로그 16편 중 5편만 공개**되어 있습니다(`published !== false`). 나머지는 `published: false`로 사이트·사이트맵에서 제외됩니다.

**연결 가능한 공개 글 (2026-08-25 기준 실측)**

- `/blog/loan-interest-calculation`
- `/blog/equal-payment-vs-equal-principal`
- `/blog/jeonse-vs-wolse`
- `/blog/acquisition-tax-guide`
- `/blog/property-yield-guide`

> 과거에 `fix(blog): 공개 글의 비공개 추천 링크 교체` 커밋이 실제로 필요했습니다. **`relatedGuides`에 넣기 전 `data/blogPosts.ts`에서 `published` 값을 확인하세요.** 비공개 글로 링크하면 죽은 내부 링크가 됩니다.
>
> LTV·실투자금·환전에 정확히 대응하는 공개 가이드는 **현재 없습니다.** `relatedGuides`를 빈 배열로 두는 것이 비공개 글에 링크하는 것보다 낫습니다(전월세 전환율 페이지가 이미 `relatedGuides={[]}`로 처리). 블로그 확충은 `PLAN.md`의 소관이며 이 문서에서 다루지 않습니다.

### 10.5 ✅ 홈 정보 구조 개편 (2026-08-28)

홈이 "계산기 카드 나열"에서 "목적별 의사결정 허브"로 바뀌었습니다.

#### 개편 전 문제 (실측)

```text
H2 7개    지금 바로 계산하기 / 인기 계산기 / 대출 / 부동산 / 금융 / 가이드 / FAQ
H3 27개
링크 반복  대출이자 ×5 · 취득세 ×5 · 원리금상환 ×4 · 월세vs전세 ×4
```

`지금 바로 계산하기`와 `인기 계산기`는 **같은 계산기 4개**를 순서만 바꿔 반복했습니다.
계산기 데이터가 5개 배열(`QUICK_CALCS`·`POPULAR`·`LOAN_CALCS`·`REALESTATE_CALCS`·
`FINANCE_CALCS`)에 중복 등록돼 설명 문구가 배열마다 달랐고, 2년 된 계산기에 `신규`
배지가 붙어 있었습니다.

#### 개편 후

```text
H2 6개    어떤 상황이신가요? / 주택 구입은 네 단계로 / 금융 가이드 /
          전체 계산기 둘러보기 / 자주 묻는 질문 / 숫자를 어떻게 관리하는지
H3 10개
링크 반복  DSR·LTV·실투자금·수익률 각 ×2 (목적 카드 + 자금 흐름 단계)
```

- **단일 데이터 소스** — `src/data/homeNav.ts` 의 `CALC` 한 곳. 섹션들은 키만 참조합니다.
- **배지 제거** — `인기·추천·신규`를 뒷받침할 실측 데이터가 없습니다.
- **카드형 계산기 6개** — DSR·LTV·대출이자·실투자금·월세vs전세·환전.
  나머지는 목적 그룹 안의 칩 링크와 카테고리 허브 3개로만 노출합니다.
- **전체 목록 제거** — Footer 가 이미 전량을 담고 있어 홈은 진입점까지만 제공합니다.

#### 대출 한도 표현 — 단정을 피한다

`실제 한도는 DSR·LTV 중 낮은 쪽`은 **너무 단정적**입니다. DSR·LTV 는 규제상
상한 축이고, 실제 한도는 금융회사 심사·상품별 한도·담보 평가에 따라 더 낮아집니다.

홈 3곳(FAQ · 목적별 안내 · 자금 흐름)을 아래 문구로 통일했습니다.

> DSR과 LTV 기준을 **모두 충족**해야 하며, 금융회사의 심사 결과에 따라
> 실제 한도는 **더 낮을 수 있습니다.**

> ⚠️ **같은 표현이 홈 밖 6곳에 남아 있습니다.** 이번 작업은 홈으로 한정했습니다.
>
> ```text
> src/app/loan/page.tsx:321
> src/app/real-estate/page.tsx:183
> src/app/ltv-calculator/page.tsx:62, 94
> src/app/dsr-calculator/page.tsx:242
> src/components/calculator/InitialCostCalc.tsx:493
> src/components/calculator/LtvCalc.tsx:278
> src/components/calculator/DsrCalc.tsx:540
> ```
>
> `ltv-calculator/page.tsx:62` 은 이미 "심사에 따라 더 낮아질 수 있다"는 단서가
> 붙어 있습니다. 나머지는 단서 없이 단정형이라 통일이 필요합니다.

#### 계산기 개수를 적지 않는다

`27개`, 카테고리별 `10·7·10개`를 하드코딩했다가 제거했습니다. 전체 계산기를
한곳에서 세는 레지스트리가 없어, 계산기를 추가하면 그대로 낡습니다.
레지스트리가 생기면 그때 자동 산출해서 표기합니다.

#### 목적 그룹은 대표 CTA 하나

같은 크기 카드를 여러 개 두면 "무엇부터 눌러야 하나"를 사용자가 다시 고민하게
됩니다. 홈의 목적이 그 고민을 없애는 것이므로 그룹마다 **대표 CTA 1개 + 보조
링크**로 바꿨습니다.

| 그룹 | 대표 | 보충 안내 |
|---|---|---|
| 얼마나 빌릴 수 있는지 | **DSR 계산기** | 주택담보대출이라면 LTV 도 함께 확인 |
| 집을 사거나 임대 투자 | **실투자금 계산기** | 매매가만으로는 부족하다는 점 |
| 저축하고 현금 관리 | **목표 저축 계산기** | 목표·기간에서 월 납입액 역산 |

저축 그룹의 대표를 목표 저축으로 둔 이유는, 저축의 출발점이 "얼마를 모아야
하나"이기 때문입니다. 예·적금 이자 계산은 목표가 정해진 뒤의 단계입니다.

#### 남은 중복 4건은 의도한 것

`DSR·LTV·실투자금·수익률` 이 각각 2회 등장합니다. 목적별 카드(탐색)와 자금 흐름
단계(순서 설명)는 역할이 다르고, 자금 흐름은 이 사이트의 차별점이라 링크가 필요합니다.
개편 전 최대 5회에서 2회로 줄인 상태이며, 더 줄이려면 자금 흐름에서 링크를 빼야 하는데
그러면 단계별 진입이 막힙니다.

#### 값 인계 범위를 화면에 정확히 적었다

자동으로 다 이어지는 것처럼 보이면 안 되므로 단계마다 실제 동작을 적었습니다.

```text
1 DSR  → 2 LTV        입력값 이어지지 않음 (요구 입력이 겹치지 않아 넘길 값이 없음)
2 LTV  → 3 실투자금    주택가격·담보 기준 한도 인계
3 실투자금 → 4 수익률   매입가·대출·보증금·부대비용 인계
```

---

### 10.6 SEO 후속 항목 — 이번 작업과 분리

홈 개편과 섞지 않고 별도로 처리할 항목입니다.

| 항목 | 현재 상태 | 비고 |
|---|---|---|
| **Open Graph 대표 이미지** | `twitter:card: summary_large_image` 는 설정돼 있으나 **실제 이미지 없음** (`public/` 에 `ads.txt`·`favicon.ico` 뿐) | 카드 규격 이미지 제작이 선행. 이번 작업에서 억지로 만들지 않음 |
| **사이트맵 실제 수정일** | 일반 페이지가 `lastModified: new Date()` — 빌드할 때마다 전부 "오늘 수정"으로 나감 | 블로그는 `post.date` 를 쓰고 있어 정상. 일반 페이지도 실제 변경일이 필요 |
| **블로그 작성자·검토일·출처** | 없음 | YMYL 금융 글에 필요한 E-E-A-T 신호 |
| **Article 구조화 데이터** | 없음 | 블로그 글에 `Article`/`author`/`datePublished` |
| **AdSense CMP 설정** | 확인 불가 | AdSense 계정에서 확인해야 하는 외부 항목 |

---

## 11. 미확정 사항 — 개발 착수 전 결정이 필요한 질문

### Q1. LTV 정책값을 누가, 어디까지 확정할 것인가 ⛔ **PR 1 착수 차단 요인**

LTV 계산기 작업의 대부분은 코드가 아니라 **정책 테이블 확정**입니다. 결정할 것:

1. **지역 구분 축** — 규제지역/비규제 2분법인가, 수도권/지방 축을 함께 쓰는가? (DSR은 `metro`/`local` 2분법을 씀 — 같은 축을 쓰면 코드가 일관되지만 LTV 정책이 요구하는 축과 다를 수 있음)
2. **v1 지원 범위** — 주택담보대출만? 생애최초·서민실수요자 우대까지? 처분조건부 1주택까지?
3. **방공제** — v1에 포함? 지역별 최우선변제금 테이블까지 넣을 것인가, "MCI 가입 시 미적용" 토글로 단순화할 것인가?
4. **정책 절대 한도** — 현행 한도 규정을 반영할 것인가, 반영한다면 어떤 조건에 적용되는지까지 분기할 것인가?

> 권고: `dsr.ts`가 v1을 "주담대 전용 · 변동/순수고정만 · 혼합형 미지원"으로 **좁게 자르고 나머지를 주석에 v2 스펙으로 남긴** 방식이 잘 작동했습니다. LTV도 같은 방식으로 **좁게 시작**할 것을 권합니다.

### Q2. 테스트 러너를 이번에 도입할 것인가

현재 테스트가 0건입니다. LTV·실투자금·환전은 경계값이 많아 회귀 위험이 기존 계산기보다 큽니다.

- **A안 — 도입하지 않음**: 이번 범위에 집중. EXAMPLES 수치를 손으로 검산해 회귀 기준으로 사용.
- **B안 — Vitest 최소 도입**: 신규 엔진 3개만 대상. devDependency 1~2개 추가. "불필요한 패키지 업그레이드 금지" 원칙과 충돌하지는 않으나 **범위 확대**임.

> 이 문서는 어느 쪽도 전제하지 않았습니다. 9.3절이 두 경우를 모두 기술합니다.

### Q3. 환전 계산기의 환율 데이터 (5.4절)

A(사용자 직접 입력) / B(빌드타임 fetch + ISR) / C(클라이언트 fetch) 중 선택.
**B안을 고르면 "정적 사이트" 전제가 깨지고 배포 인프라 확인이 필요**하므로 이번 확장 범위를 넘어섭니다. → 권장 A안, 결정 필요.

### Q4. 환전 계산기를 어느 그룹에 넣을 것인가

현재 `/finance` 그룹은 예·적금·복리·인플레이션 등 **저축·자산 축**입니다. 환전은 여기에 완전히 들어맞지 않습니다.

- **A안** `/finance/exchange` — 새 그룹 안 만들고 기존 금융 허브에 편입 (가장 단순)
- **B안** `/exchange-calculator` 루트 평면 경로 — 대출 계산기 관례를 따르되 허브 소속이 애매
- **C안** 새 허브 `/currency` 또는 `/living` 신설 — 향후 확장 여지는 크지만 **계산기 1개짜리 허브**가 됨

> 권장 A안. 계산기 1개를 위해 4번째 허브를 만들면 Header 네비게이션이 무거워집니다.

### Q5. 실투자금 계산기의 URL

- `/real-estate/initial-cost-calculator` — "초기 비용"에 가까움 (문서 본문 채택안)
- `/real-estate/actual-investment-calculator` — "실투자금"의 직역
- `/real-estate/purchase-cost-calculator` — "매수 비용"

> URL은 배포 후 변경 비용이 큽니다(리다이렉트 관리·색인 재수집). **PR 2 착수 전에 확정**하세요.

### Q6. DSR v2를 이번 확장에 포함할 것인가

신규 URL이 없어 검색 유입 증가가 없고, 정책 리스크는 가장 큽니다. 반면 스펙이 이미 코드에 작성돼 있어 착수 비용은 낮습니다.
→ **신규 계산기 2개(PR 1·2)를 먼저 배포하고 색인 상황을 본 뒤 판단**하는 것을 권합니다.

### Q7. (확장과 무관, 그러나 시급) 금통위 2026-08-27 대응

`lib/realEstate.ts`의 `CONVERSION_RATE_INFO.baseRatePct = 2.75` (검증일 2026-08-15).
**이틀 뒤 금통위에서 기준금리가 바뀌면 전월세 전환율 계산기의 법정 상한이 즉시 틀립니다.**
확장 작업과 별개로 8/27 이후 확인·갱신이 필요합니다. → `fix(realestate): 기준금리 변경 반영` 형태의 독립 커밋.

---

## 12. 첫 번째 구현 작업의 완료 조건

**대상: PR 0 (공통화) + PR 1 (LTV 계산기)** — Q1이 해결되지 않으면 PR 0 + PR 2(실투자금)로 대체합니다.

### PR 0 완료 조건

- [ ] `src/components/calculator/ToggleGroup.tsx`가 존재하고 `index.ts`에서 export된다
- [ ] `DsrCalc.tsx`가 내부 정의 대신 공통 `ToggleGroup`을 import한다
- [ ] `src/lib/policy/types.ts`에 `PolicyMeta` / `PolicyTable`이 정의되어 있다
- [ ] `src/components/calculator/PolicyNote.tsx`가 `PolicyMeta`를 받아 기준일·출처를 렌더한다
- [ ] `useCalcState.ts`가 `readRaw` / `readWon` / `isFilled`를 export하며, **훅 내부 로직은 변경되지 않았다**
- [ ] `git diff --stat` 기준 기존 24개 계산기의 **동작 변경이 0건**이다 (import 경로 변경만 허용)
- [ ] `tsc --noEmit` 에러 0 / `eslint .` 에러 0 / `next build` 성공
- [ ] DSR 계산기를 브라우저에서 열어 토글 4개가 이전과 동일하게 동작한다

### PR 1 (LTV) 완료 조건

**정책·정확성**

- [x] `src/lib/policy/ltv.ts`에 정책 테이블이 있고, `PolicyMeta`의 `verifiedAt` · `source`가 **실제로 원문을 확인한 날짜와 문서명**으로 채워져 있다
- [x] 적용 LTV가 상수가 아니라 **(지역 × 주택수 × 생애최초 × 가격구간) 조회 함수**로 구현되어 있다
- [x] 정책 테이블에 없는 조건 조합에서 **임의 보간하지 않고** "지원하지 않음"을 반환한다
- [x] `src/lib/ltv.ts`의 계산 함수가 순수 함수이며 정책 모듈만 참조한다 (React·DOM 의존 0)

**계산 정확성**

- [x] 9.4절 LTV 경계값 4종이 손 검산과 일치한다 (검산 과정을 PR 설명에 기록)
- [x] 방공제+선순위가 담보인정액을 넘을 때 음수가 아니라 `0`이 나온다
- [x] EXAMPLES 2건의 수치가 실제 계산기 출력과 일치한다

**표현**

- [x] 결과 문구가 "예상/참고/추정"이며 "대출 가능액 확정" 류 표현이 없다
- [x] `PolicyNote`로 기준일·출처가 화면에 표시된다
- [x] "실제 한도는 DSR·소득 인정방식·금융회사 심사에 따라 낮아질 수 있음" 고지가 있다
- [x] 법률·금융 판단을 대신한다는 표현이 없다

**통합**

- [x] `useMemo` 안에서 `getWon/getNum`이 아니라 `readWon/readRaw`를 사용한다 (3.7절 함정)
- [x] 등록 6곳 + Footer + `/dsr-calculator` 역링크가 모두 반영되었다
- [x] `relatedGuides`가 **공개 상태인 글만** 참조한다 (없으면 빈 배열)
- [x] `sitemap.ts`에 `ltv-calculator`가 `priority 0.9 / monthly`로 등록되었다

**검증**

- [x] `tsc --noEmit` 에러 0
- [x] `eslint .` 에러 0
- [x] `next build` 성공 + **정적 페이지 수가 직전 대비 1 증가**
- [x] 브라우저 스모크 5종 통과
  - 초기 렌더 / 마지막 입력 즉시 반영 / URL 파라미터 직접 진입 시 값 복원 / 뒤로·앞으로 상태 복원 / 모바일에서 표 가로 스크롤
- [x] 콘솔 에러 0

**문서**

- [x] 커밋 메시지가 `feat(loan): LTV 계산기 추가` 형식이다
- [x] `PLAN.md` · `claude/UPGRADE-2026-08.md`가 **변경되지 않았다**
- [x] 이 문서(7절)의 PR 1 체크박스가 갱신되었다

---

## 부록 A. 조사 근거

이 문서의 "현재 구현 현황"은 다음 실측에 근거합니다.

| 확인 항목 | 방법 | 결과 |
|---|---|---|
| 라우팅·페이지 목록 | `find src -type f` | 계산기 24개, 허브 3개 |
| 계산 엔진 | `src/lib/*.ts` 전문 확인 | 5개 파일 |
| 공통 컴포넌트 | `components/calculator/*` 전문/행수 확인 | 30개 파일, 5,125행 |
| 중복 여부 | `grep -rn "환전\|exchange\|LTV\|실투자금"` | 환전 0건, LTV는 입력 필드로만 존재 |
| 테스트 | 테스트 파일·러너 탐색 | 0건 |
| 문서 관례 | `git log --diff-filter=A -- "*.md"` | `README.md` 1건뿐 |
| 커밋 관례 | `git log --oneline -30` | `타입(스코프): 한글 설명`, 계산기 1개 = 커밋 1개 |
| 블로그 공개 상태 | `grep "slug:\|published:" src/data/blogPosts.ts` | 16편 중 5편 공개 |
| 정책값 현황 | `dsr.ts` · `realEstate.ts` 상수·주석 | 검증일 3건, 형식 2종 |

**의도적으로 포함하지 않은 것**: 검색량·시장 규모 수치(실측 데이터 없음), LTV·중개보수의 구체적 정책 수치(착수 시점 원문 확인 필요), 코드 구현(이번 단계 범위 밖).
